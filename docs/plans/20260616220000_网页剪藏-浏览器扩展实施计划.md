# 网页剪藏 - 浏览器扩展 实施计划（Plan 2）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **本环境说明:** 当前会话子代理模型路由异常（子代理不可用），如在本环境执行请走 **Inline Execution（superpowers:executing-plans）**，由主会话逐 Task 实现。

**Goal:** 新建 `extension/` 浏览器扩展工程（Manifest V3，Chrome + Edge 通用），在任意网页一键抓取内容+图片，调用 Plan 1 已就绪的 `POST /api/tasks/clip` 拆解成任务草案，侧边栏内编辑确认后批量建到 AI Task Flow 看板；侧边栏做成多视图宿主，为对话等未来视图预留注册接口。

**Architecture:** 扩展是独立工程（不复用 frontend 代码）。采集 = 动态注入的 content script（activeTab 触发，不声明 `<all_urls>`，无高权限警告）划词优先、否则 `@mozilla/readability` 提正文，`<img>`→fetch→base64；拆解/建任务由宿主后端负责（Plan 1 已完成），扩展只做「采集 + 转码 + 草案确认」。content script 算出 `PageContext` 后经 `chrome.runtime.sendMessage` 回传侧栏（不依赖 executeScript 返回值机制，最稳）。侧栏是**多视图宿主**：加功能 = `registry.push(一项)`，容器逻辑不动。

**Tech Stack:** TypeScript（strict）+ React 18 + Vite 5.0.11 + `@crxjs/vite-plugin@2.5.0`（MV3 打包）+ `@mozilla/readability`（正文提取）+ vitest/jsdom（纯逻辑测试）+ `@ai-task-flow/shared`（仅 `import type`，无运行时耦合）+ 原生 CSS + CSS 变量（设计 Token，不引入 Tailwind 以降低 crxjs 集成风险）。

**关联设计文档:** `docs/20260616203000_网页内容剪藏插件设计.md`（§3.1 扩展 / §3.5 多视图宿主 / §4 数据契约 / §6 错误处理 / §7 测试）。
**前置依赖:** Plan 1（`docs/plans/20260616213000_网页剪藏-宿主改造实施计划.md`）已完成 —— `POST /api/tasks/clip` 与 `source`/`sourceUrl` 字段已就绪。

---

## 文件结构总览

**新建（Create，全部在 `extension/` 下）：**
| 文件 | 职责 |
|------|------|
| `extension/package.json` | 扩展独立包，声明依赖与 scripts |
| `extension/tsconfig.json` | strict + jsx react-jsx + chrome 类型 |
| `extension/vite.config.ts` | crxjs + react 构建配置 |
| `extension/vitest.config.ts` | vitest + jsdom（与 vite 配置分离，避免 crxjs 进测试） |
| `extension/manifest.json` | MV3 配置（sidePanel/activeTab/storage/scripting；host 仅 localhost:3000） |
| `extension/index.html` | 占位（crxjs 入口由 manifest 指定 sidepanel/index.html，此文件可不要；保留以防） |
| `extension/src/types/pageContext.ts` | `PageContext`（扩展内部类型，不入 shared 契约） |
| `extension/src/content/capture.ts` | **核心采集**：`capturePageContext(opts)`，划词优先/否则 Readability（纯逻辑，jsdom 可测） |
| `extension/src/content/images.ts` | `collectImages(root)`：`<img>`→base64，命名 img-1..N，CORS 失败跳过 |
| `extension/src/content/clip.ts` | 动态注入入口：采集 + `sendMessage({type:'CAPTURE_RESULT'})` 回传 |
| `extension/src/background/index.ts` | service worker：`openPanelOnActionClick`（点图标开侧栏，触发 activeTab） |
| `extension/src/sidepanel/index.html` | 侧栏入口 HTML |
| `extension/src/sidepanel/main.tsx` | React 根 + Provider |
| `extension/src/sidepanel/styles.css` | 原生 CSS + 设计 Token（含暗色模式） |
| `extension/src/sidepanel/SidePanel.tsx` | **容器**：Tab 切换 + 视图路由 + 底部状态；当前 Tab 记忆到 `chrome.storage` |
| `extension/src/sidepanel/PageContextStore.tsx` | 共享 `PageContext` 的 React Context + `onMessage` 监听 |
| `extension/src/sidepanel/views/types.ts` | 视图契约 `interface SidePanelView` |
| `extension/src/sidepanel/views/registry.ts` | **视图注册表（扩展点）** `VIEWS: SidePanelView[]` |
| `extension/src/sidepanel/views/ClipView.tsx` | **剪藏视图**：抓取→草案预览→编辑→批量建任务 |
| `extension/src/sidepanel/views/ChatView.tsx` | **对话视图（预留 stub）** |
| `extension/src/sidepanel/components/DraftCard.tsx` | 单条草案编辑器（标题/描述/勾选步骤/图） |
| `extension/src/sidepanel/api/backend.ts` | `captureToDrafts` / `createTaskFromDraft`（fetch 调宿主） |
| `extension/src/content/__tests__/capture.test.ts` | 采集纯逻辑测试（jsdom） |
| `extension/src/content/__tests__/images.test.ts` | 图片采集测试（jsdom + fetch mock） |
| `extension/src/sidepanel/__tests__/PageContextStore.test.tsx` | Context store 测试（chrome mock） |
| `extension/src/sidepanel/api/__tests__/backend.test.ts` | API client 测试（fetch mock） |

**修改（Modify）：**
| 文件 | 改动 |
|------|------|
| `package.json`（根） | `workspaces` 数组加 `"extension"`；加 `dev:extension` / `build:extension` 脚本 |

---

## 关键设计约定（贯穿所有 Task，避免不一致）

1. **MV3 权限**：`permissions: [sidePanel, activeTab, storage, scripting]`；`host_permissions: ["http://localhost:3000/*"]`（仅宿主）；**不声明 `<all_urls>`**，靠 activeTab 在用户点图标时获得当前页临时抓取权限 → 安装时无「读取所有网站」高权限警告（设计 §3.1）。
2. **抓取链路**：`ClipView`「抓取本页」→ `chrome.scripting.executeScript({target, files})` 注入 `content/clip` → 在页面上下文算 `PageContext` → `chrome.runtime.sendMessage({type:'CAPTURE_RESULT', payload})` 回传 → `PageContextStore` 的 `onMessage` 监听存入 Context → `ClipView` `useEffect` 监听 Context 变化触发拆解。**不依赖 executeScript 返回值**（改用消息，规避其语义不确定性）。
3. **图片命名**：按 DOM 顺序 `img-1..N`；`fetch`→base64；**单张跨域失败跳过**（不产生 ClipImage 条目，不阻断）；失败图原序号留空，后续图仍按原序号命名 → manifest 不会误导 LLM 引用不存在的图。`content.text` 末尾附「（图片：img-1、img-2…）」清单供 LLM 按名引用。
4. **建任务**：每条 draft → `POST /api/tasks`，固定 `source:'web'` + `sourceUrl` + `prefix`（**默认 `'WEB'`**，ClipView 可改）+ `steps`（图已是 `/api/uploads/xxx`，原样透传）。**prefix 必填**（后端 `TaskId.create` 需要），用 `'WEB'` 作为剪藏任务 ID 序列。
5. **类型耦合**：`PageContext` 是扩展内部类型；shared 契约（`ClipRequest`/`ClipDraft`/`ClipResponse`/`CreateTaskRequest`）一律 `import type` → 构建产物不含 shared 运行时代码（shared 仅需产出 `.d.ts`）。执行前确保 `npm run build:shared` 已跑过（Plan 1 已 build）。
6. **多视图宿主**：`registry.ts` 导出 `VIEWS`，加功能 = `VIEWS.push({...})`，`SidePanel` 容器读 `VIEWS` 渲染 Tab，容器永不改（设计 §3.5）。
7. **测试纪律**：纯逻辑（capture/images/store/api client）用 vitest+jsdom 写单测；UI 组件（ClipView/DraftCard/SidePanel）、background、content 入口靠**手动 E2E 验证**（诚实声明，同 Plan 1 Task 10/11）。
8. **构建版本锁**：`vite@~5.0.11` + `@crxjs/vite-plugin@2.5.0`。**crxjs 在 vite > 5.0.11 有构建 bug（issue #874）**，切勿升 vite 小版本。
9. **样式**：原生 CSS + CSS 变量（设计 Token，含暗色），**禁硬编码颜色**（遵循项目规范）。不引入 Tailwind（降低 crxjs 集成面）。

---

## Task 1: extension 工程脚手架 + 构建链

**Files:**
- Create: `extension/package.json`
- Create: `extension/tsconfig.json`
- Create: `extension/vite.config.ts`
- Create: `extension/vitest.config.ts`
- Create: `extension/manifest.json`
- Create: `extension/src/background/index.ts`（最小占位，Task 5 填充）
- Create: `extension/src/sidepanel/index.html`（最小占位，Task 5 填充）
- Create: `extension/src/content/clip.ts`（最小占位，Task 4 填充）
- Modify: `package.json`（根）

- [ ] **Step 1: 创建 `extension/package.json`**

```json
{
  "name": "@ai-task-flow/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI Task Flow 网页剪藏浏览器扩展（MV3）",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-task-flow/shared": "^0.2.0",
    "@mozilla/readability": "^0.5.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "2.5.0",
    "@testing-library/react": "^14.3.1",
    "@types/chrome": "^0.0.260",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "jsdom": "^24.1.0",
    "typescript": "^5.3.3",
    "vite": "~5.0.11",
    "vitest": "^1.6.0"
  }
}
```

> **版本锁要点**：`vite` 必须 `~5.0.11`（crxjs 2.5.0 在 vite > 5.0.11 构建失败，issue #874）。若 `npm install` 报版本冲突，**不要升 vite**，上网核对 crxjs 当前推荐组合。

- [ ] **Step 2: 创建 `extension/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "strict": true,
    "types": ["chrome", "vitest/globals"],
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src", "manifest.json"]
}
```

- [ ] **Step 3: 创建 `extension/vite.config.ts`**

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { crx } from '@crxjs/vite-plugin';
import manifest from './manifest.json';

export default defineConfig({
  plugins: [react(), crx({ manifest })],
  // crxjs HMR 端口固定，避免每次变
  server: { port: 5174, strictPort: true, hmr: { port: 5174 } },
  build: { outDir: 'dist', emptyOutDir: true },
});
```

- [ ] **Step 4: 创建 `extension/vitest.config.ts`（与 vite 配置分离，测试不带 crxjs）**

```ts
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    include: ['src/**/*.test.ts', 'src/**/*.test.tsx'],
  },
});
```

- [ ] **Step 5: 创建 `extension/manifest.json`**

```json
{
  "manifest_version": 3,
  "name": "AI Task Flow 网页剪藏",
  "version": "0.1.0",
  "description": "抓取网页内容与图片，AI 拆解成任务草案，侧边栏确认后建到 AI Task Flow 看板。",
  "permissions": ["sidePanel", "activeTab", "storage", "scripting"],
  "host_permissions": ["http://localhost:3000/*"],
  "background": { "service_worker": "src/background/index.ts", "type": "module" },
  "side_panel": { "default_path": "src/sidepanel/index.html" },
  "action": { "default_title": "打开 AI Task Flow 剪藏侧栏" },
  "web_accessible_resources": [
    { "resources": ["src/content/clip.ts"], "matches": ["http://*/*", "https://*/*"] }
  ]
}
```

> **说明**：`web_accessible_resources` 声明 `clip.ts` 让 crxjs 把它编译进产物（crxjs 自动编译 TS 并加入 WAR，见 crxjs 文档/content-scripts）。`matches` 是 WAR 可被哪些页面引用，不影响权限提示。运行时注入路径在 Task 4 确认。

- [ ] **Step 6: 创建 3 个最小占位文件（让 Task 1 能构建过）**

`extension/src/background/index.ts`：
```ts
// 占位：Task 5 填充（openPanelOnActionClick）
export {};
```

`extension/src/sidepanel/index.html`：
```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>AI Task Flow 剪藏</title>
  </head>
  <body>
    <div id="root">（Task 5 接入 React）</div>
  </body>
</html>
```

`extension/src/content/clip.ts`：
```ts
// 占位：Task 4 填充（采集 + sendMessage 回传）
export {};
```

- [ ] **Step 7: 根 `package.json` 接入 workspace + 脚本**

把根 `package.json` 的 `workspaces` 改为：
```json
  "workspaces": ["shared", "backend", "frontend", "cli", "extension"],
```
在根 `scripts` 加：
```json
    "dev:extension": "cd extension && npm run dev",
    "build:extension": "cd extension && npm run build",
```

- [ ] **Step 8: 安装依赖 + 确保 shared 已构建**

Run（项目根）：
```bash
npm install
npm run build:shared
```
Expected：`extension/node_modules` 建好；`shared/dist/index.d.ts` 存在（供 `import type` 解析）。

- [ ] **Step 9: 构建验证 + 记录 content 产物路径**

Run：`cd extension && npm run build`
Expected：产出 `extension/dist/`，含 `manifest.json`、background、sidepanel 入口。

**关键**：列出 `dist/` 找到 `clip` 的输出路径（crxjs 通常输出为 `src/content/clip.js`，保留源 `src/` 前缀）：
```bash
ls extension/dist   # 或 find extension/dist -name 'clip*'
```
**记录该路径**（下文记为 `$CLIP_PATH`，预期 `src/content/clip.js`），Task 4 的 `executeScript({files})` 用它。若实际路径不同，Task 4 按实际调整。

- [ ] **Step 10: Commit**
```bash
git add extension package.json
git commit -m "feat(extension): 工程脚手架(MV3+crxjs+vite5.0.11)+workspaces 接入"
```

---

## Task 2: PageContext 类型 + capturePageContext 纯逻辑（划词优先/Readability）

**Files:**
- Create: `extension/src/types/pageContext.ts`
- Create: `extension/src/content/capture.ts`
- Create: `extension/src/content/__tests__/capture.test.ts`

- [ ] **Step 1: 创建 `extension/src/types/pageContext.ts`**

```ts
import type { ClipImage } from '@ai-task-flow/shared';

/**
 * 抓取到的页面上下文（扩展内部类型，剪藏/对话视图共用）。
 * 不入 shared 契约——它是扩展的采集产物，宿主后端只消费其中的 ClipRequest 子集。
 */
export interface PageContext {
  sourceUrl: string;   // location.href
  title: string;       // document.title
  text: string;        // 选区文本 或 Readability 正文（末尾附图片清单）
  images?: ClipImage[]; // wantImages 时为 img-1..N 的 base64
}
```

- [ ] **Step 2: 先写失败测试 `capture.test.ts`**

```ts
// extension/src/content/__tests__/capture.test.ts
import { describe, it, expect } from 'vitest';
import { capturePageContext } from '../capture.js';

describe('capturePageContext', () => {
  it('should return selection text when there is a selection', async () => {
    document.body.innerHTML =
      '<div><p id="a">审核判定页面文本未国际化</p><p>其他无关内容</p></div>';
    const range = document.createRange();
    range.selectNodeContents(document.getElementById('a')!);
    const selection = window.getSelection()!;
    selection.removeAllRanges();
    selection.addRange(range);

    const ctx = await capturePageContext();
    expect(ctx.text).toContain('审核判定页面文本未国际化');
    expect(ctx.text).not.toContain('其他无关内容');
    expect(ctx.sourceUrl).toBe(location.href);
  });

  it('should fall back to body text when no selection', async () => {
    document.body.innerHTML =
      '<article><h1>标题</h1><p>这是一段正文内容用于回退提取。</p></article>';
    window.getSelection()?.removeAllRanges();

    const ctx = await capturePageContext();
    // Readability 提取；若 jsdom 下 Readability 返回空则回退 body.textContent。两者都应含正文关键词。
    expect(ctx.text).toContain('正文内容');
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run：`cd extension && npx vitest run src/content/__tests__/capture.test.ts`
Expected：FAIL（`capturePageContext` 不存在 / `../capture.js` 找不到）。

- [ ] **Step 4: 实现 `extension/src/content/capture.ts`**

```ts
// extension/src/content/capture.ts
import { Readability } from '@mozilla/readability';
import type { ClipImage } from '@ai-task-flow/shared';
import type { PageContext } from '../types/pageContext.js';
import { collectImages } from './images.js';

export interface CaptureOptions {
  wantImages?: boolean;
}

/**
 * 页面上下文采集（在页面上下文执行，不依赖 chrome API，便于 jsdom 测试）。
 * 划词优先：有选区取选区文本 + 选区内图片；否则 Readability 提取正文 + 全页图片。
 */
export async function capturePageContext(opts: CaptureOptions = {}): Promise<PageContext> {
  const selection = window.getSelection();
  const hasSelection = !!(selection && selection.rangeCount && selection.toString().trim());

  let text: string;
  let imageRoot: ParentNode;
  if (hasSelection) {
    text = selection!.toString().trim();
    // cloneContents() 返回选区内的 DocumentFragment（含圈住的 img）
    imageRoot = selection!.getRangeAt(0).cloneContents();
  } else {
    // Readability 在页面克隆文档上解析，避免污染原 DOM
    const article = new Readability(document.cloneNode(true) as Document).parse();
    text = (article?.textContent ?? document.body?.textContent ?? '').trim();
    imageRoot = document.body ?? document.documentElement;
  }

  const images: ClipImage[] | undefined = opts.wantImages ? await collectImages(imageRoot) : undefined;

  // 末尾附图片清单，供 LLM 按 img-N 名称引用（设计 §5）
  const manifest = images && images.length > 0
    ? `\n\n（本页含图片：${images.map((i) => i.name).join('、')}）`
    : '';

  return {
    sourceUrl: location.href,
    title: document.title,
    text: manifest ? `${text}${manifest}` : text,
    images,
  };
}
```

> 注：`capture.ts` 依赖 `./images.js`（Task 3 实现）。Task 2 跑测试时若 `images.js` 不存在会报错——**先把 Task 3 的 images.ts 创建（哪怕空 stub 导出 collectImages）**，或按 Task 2 测试用例 `wantImages` 默认 false 走不到 collectImages 调用（但 import 顶层解析仍需文件存在）。**建议执行顺序：先建 Task 3 的 images.ts，再跑 Task 2 测试**（两 Task 紧邻）。

- [ ] **Step 5: 跑测试确认通过**

Run：`cd extension && npx vitest run src/content/__tests__/capture.test.ts`
Expected：2 个测试 PASS。（若 Readability 在 jsdom 下返回空导致第二条 FAIL，确认 capture.ts 回退走了 `document.body.textContent`——jsdom 下 Readability 对极简 article 可能返回 null，回退路径已覆盖；若仍失败，把测试 article 加一段更长正文再跑。）

- [ ] **Step 6: Commit**
```bash
git add extension/src/types/pageContext.ts extension/src/content/capture.ts extension/src/content/__tests__/capture.test.ts
git commit -m "feat(extension): capturePageContext 采集逻辑(划词优先/Readability 回退)"
```

---

## Task 3: 图片采集（img→base64，CORS 跳过）

**Files:**
- Create: `extension/src/content/images.ts`
- Create: `extension/src/content/__tests__/images.test.ts`

> 执行顺序：本 Task 在 Task 2 Step 4 之前先建 `images.ts`（capture.ts 依赖它）。

- [ ] **Step 1: 先写失败测试 `images.test.ts`**

```ts
// extension/src/content/__tests__/images.test.ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { collectImages } from '../images.js';

beforeEach(() => {
  // mock fetch → 返回 Blob，供 FileReader 转 base64
  global.fetch = vi.fn(async () => ({
    blob: async () => new Blob([new Uint8Array([0x89, 0x50, 0x4e, 0x47])], { type: 'image/png' }),
  })) as unknown as typeof fetch;
});

describe('collectImages', () => {
  it('should convert images to base64 and name them img-1..N in DOM order', async () => {
    document.body.innerHTML = '<img src="https://a.com/1.png" /><img src="https://a.com/2.png" />';
    const images = await collectImages(document.body);
    expect(images).toHaveLength(2);
    expect(images[0].name).toBe('img-1');
    expect(images[1].name).toBe('img-2');
    expect(images[0].base64).toMatch(/^data:image\/png;base64,/);
  });

  it('should skip images whose fetch fails (CORS) without aborting', async () => {
    (global.fetch as ReturnType<typeof vi.fn>).mockImplementationOnce(async () => {
      throw new Error('CORS blocked');
    });
    document.body.innerHTML = '<img src="https://a.com/bad.png" /><img src="https://a.com/ok.png" />';
    const images = await collectImages(document.body);
    // 第一张失败被跳过，第二张仍按原 DOM 序号命名 img-2（保证 manifest 不误导 LLM）
    expect(images).toHaveLength(1);
    expect(images[0].name).toBe('img-2');
    expect(images[0].base64).toMatch(/^data:/);
  });

  it('should dedupe identical srcs', async () => {
    document.body.innerHTML = '<img src="https://a.com/dup.png" /><img src="https://a.com/dup.png" />';
    const images = await collectImages(document.body);
    expect(images).toHaveLength(1);
    expect(images[0].name).toBe('img-1');
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run：`cd extension && npx vitest run src/content/__tests__/images.test.ts`
Expected：FAIL（`collectImages` 不存在）。

- [ ] **Step 3: 实现 `extension/src/content/images.ts`**

```ts
// extension/src/content/images.ts
import type { ClipImage } from '@ai-task-flow/shared';

/** 单张图片 URL → base64 data URL；失败（跨域/CORS）抛错由调用方跳过 */
async function imgToBase64(src: string): Promise<string> {
  const response = await fetch(src);
  const blob = await response.blob();
  return await new Promise<string>((resolve, reject) => {
    const reader = new FileReader();
    reader.onloadend = () => resolve(reader.result as string);
    reader.onerror = () => reject(new Error(`读取图片失败: ${src}`));
    reader.readAsDataURL(blob);
  });
}

/**
 * 采集 root 范围内的 <img>，按 DOM 顺序命名 img-1..N，fetch→base64。
 * 跨域/失败的单张跳过（不产生条目），不阻断整体采集（设计 §6）。
 * 名字按「原始 DOM 位置序号」给，失败图留空号 → manifest 不会误导 LLM 引用不存在的图。
 */
export async function collectImages(root: ParentNode): Promise<ClipImage[]> {
  const imgs = Array.from(root.querySelectorAll('img'));
  const seen = new Set<string>();
  const srcs: string[] = [];
  for (const img of imgs) {
    const src = img.currentSrc || img.src;
    if (src && !seen.has(src)) {
      seen.add(src);
      srcs.push(src);
    }
  }

  const settled = await Promise.all(
    srcs.map(async (src, index) => {
      try {
        return { name: `img-${index + 1}`, base64: await imgToBase64(src) } as ClipImage;
      } catch {
        return null; // 跨域/读取失败：跳过该图
      }
    }),
  );
  return settled.filter((x): x is ClipImage => x !== null);
}
```

- [ ] **Step 4: 跑测试确认通过**

Run：`cd extension && npx vitest run src/content/__tests__/images.test.ts`
Expected：3 个测试 PASS。

- [ ] **Step 5: 联跑 Task 2 测试确认无回归**

Run：`cd extension && npx vitest run src/content`
Expected：capture + images 全部 PASS。

- [ ] **Step 6: Commit**
```bash
git add extension/src/content/images.ts extension/src/content/__tests__/images.test.ts
git commit -m "feat(extension): 图片采集(img→base64,跨域跳过,按序命名)"
```

---

## Task 4: content 入口（采集→sendMessage 回传）

**Files:**
- Modify: `extension/src/content/clip.ts`（替换 Task 1 占位）

- [ ] **Step 1: 实现 `extension/src/content/clip.ts`**

```ts
// extension/src/content/clip.ts
// 动态注入入口：被 chrome.scripting.executeScript({files}) 注入到当前页，
// 在页面上下文采集 PageContext，经 chrome.runtime.sendMessage 回传侧栏。
// （不依赖 executeScript 返回值机制——改用消息，规避其语义不确定性。）
import { capturePageContext } from './capture.js';

async function main(): Promise<void> {
  try {
    const pageContext = await capturePageContext({ wantImages: true });
    chrome.runtime.sendMessage({ type: 'CAPTURE_RESULT', payload: pageContext });
  } catch (error) {
    chrome.runtime.sendMessage({ type: 'CAPTURE_ERROR', message: String(error) });
  }
}

void main();
```

- [ ] **Step 2: 确认 `$CLIP_PATH`（Task 1 Step 9 记录）**

回看 Task 1 构建产物里 clip 的输出路径（预期 `src/content/clip.js`）。后续 `executeScript({files})` 用它。若改了文件位置需重 build 确认。

- [ ] **Step 3: 类型检查**

Run：`cd extension && npx tsc --noEmit`
Expected：通过。

- [ ] **Step 4: 手动验证（content 入口需真实浏览器，无单测）**

Run：`cd extension && npm run build`，Chrome「加载已解压的扩展」选 `extension/dist/`。在任意网页 F12 控制台手动 `chrome.runtime.sendMessage` 不便——**完整抓取验证留到 Task 8 E2E**，本 Task 仅保证构建产物含 `clip.js` 且类型通过。

- [ ] **Step 5: Commit**
```bash
git add extension/src/content/clip.ts
git commit -m "feat(extension): content 入口(采集+sendMessage 回传 CAPTURE_RESULT)"
```

---

## Task 5: background（点图标开侧栏）+ 侧栏容器 + 视图注册表 + ChatView stub + ClipView 抓取桩

**Files:**
- Modify: `extension/src/background/index.ts`（替换占位）
- Create: `extension/src/sidepanel/main.tsx`
- Modify: `extension/src/sidepanel/index.html`（接入 React 入口）
- Create: `extension/src/sidepanel/styles.css`
- Create: `extension/src/sidepanel/PageContextStore.tsx`
- Create: `extension/src/sidepanel/views/types.ts`
- Create: `extension/src/sidepanel/views/registry.ts`
- Create: `extension/src/sidepanel/SidePanel.tsx`
- Create: `extension/src/sidepanel/views/ChatView.tsx`
- Create: `extension/src/sidepanel/views/ClipView.tsx`（抓取桩，Task 7 填完整）
- Create: `extension/src/sidepanel/__tests__/PageContextStore.test.tsx`

- [ ] **Step 1: 先写失败测试 `PageContextStore.test.tsx`**

```tsx
// extension/src/sidepanel/__tests__/PageContextStore.test.tsx
import { describe, it, expect, beforeEach } from 'vitest';
import { render, act } from '@testing-library/react';
import { PageContextProvider, usePageContext } from '../PageContextStore.js';

let listeners: Array<(message: { type: string; payload?: unknown; message?: string }) => void> = [];

beforeEach(() => {
  listeners = [];
  (global as unknown as { chrome: unknown }).chrome = {
    runtime: {
      onMessage: {
        addListener: (l: (m: unknown) => void) => listeners.push(l as never),
        removeListener: (l: (m: unknown) => void) => {
          listeners = listeners.filter((x) => x !== (l as never));
        },
      },
    },
  };
});

function Probe() {
  const { pageContext, error } = usePageContext();
  return <div>{pageContext ? pageContext.sourceUrl : 'empty'}|{error ?? 'no-err'}</div>;
}

describe('PageContextStore', () => {
  it('should store page context on CAPTURE_RESULT message', () => {
    const { getByText } = render(
      <PageContextProvider>
        <Probe />
      </PageContextProvider>,
    );
    expect(getByText('empty|no-err')).toBeTruthy();
    act(() => {
      listeners.forEach((l) =>
        l({ type: 'CAPTURE_RESULT', payload: { sourceUrl: 'https://x.com/p', title: 't', text: 'x' } }),
      );
    });
    expect(getByText('https://x.com/p|no-err')).toBeTruthy();
  });

  it('should set error on CAPTURE_ERROR message', () => {
    const { getByText } = render(
      <PageContextProvider>
        <Probe />
      </PageContextProvider>,
    );
    act(() => {
      listeners.forEach((l) => l({ type: 'CAPTURE_ERROR', message: '抓取失败' }));
    });
    expect(getByText('empty|抓取失败')).toBeTruthy();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run：`cd extension && npx vitest run src/sidepanel/__tests__/PageContextStore.test.tsx`
Expected：FAIL（`PageContextStore` 不存在）。

- [ ] **Step 3: 实现 `PageContextStore.tsx`**

```tsx
// extension/src/sidepanel/PageContextStore.tsx
import { createContext, useContext, useEffect, useState, type ReactNode } from 'react';
import type { PageContext } from '../types/pageContext.js';

interface PageContextValue {
  pageContext: PageContext | null;
  setPageContext: (ctx: PageContext | null) => void;
  error: string | null;
}

const Ctx = createContext<PageContextValue | null>(null);

interface CaptureMessage {
  type: 'CAPTURE_RESULT' | 'CAPTURE_ERROR';
  payload?: PageContext;
  message?: string;
}

/** 所有视图共享的页面上下文（剪藏建任务 / 未来对话喂 AI 共用同一份抓取结果） */
export function PageContextProvider({ children }: { children: ReactNode }) {
  const [pageContext, setPageContext] = useState<PageContext | null>(null);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    const listener = (message: CaptureMessage) => {
      if (message.type === 'CAPTURE_RESULT' && message.payload) {
        setPageContext(message.payload);
        setError(null);
      } else if (message.type === 'CAPTURE_ERROR') {
        setError(message.message ?? '抓取失败');
      }
    };
    chrome.runtime.onMessage.addListener(listener);
    return () => chrome.runtime.onMessage.removeListener(listener);
  }, []);

  return <Ctx.Provider value={{ pageContext, setPageContext, error }}>{children}</Ctx.Provider>;
}

export function usePageContext(): PageContextValue {
  const value = useContext(Ctx);
  if (!value) throw new Error('usePageContext 必须在 PageContextProvider 内使用');
  return value;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run：`cd extension && npx vitest run src/sidepanel/__tests__/PageContextStore.test.tsx`
Expected：2 个测试 PASS。

- [ ] **Step 5: 实现 `views/types.ts`**

```ts
// extension/src/sidepanel/views/types.ts
import type { ComponentType } from 'react';

/** 侧栏视图契约：加功能 = registry.push 一项（设计 §3.5） */
export interface SidePanelView {
  id: string;
  title: string;
  icon: string; // emoji 标识
  component: ComponentType;
}
```

- [ ] **Step 6: 实现 `views/ChatView.tsx`（预留 stub）**

```tsx
// extension/src/sidepanel/views/ChatView.tsx
export function ChatView() {
  return (
    <div className="sp-empty">
      💬 对话视图（开发中）
      <p>未来：在任意网页上一边浏览一边问 AI，默认带入「当前页面上下文」。</p>
    </div>
  );
}
```

- [ ] **Step 7: 实现 `views/ClipView.tsx`（抓取桩，Task 7 填完整）**

```tsx
// extension/src/sidepanel/views/ClipView.tsx
import { usePageContext } from '../PageContextStore.js';

const CLIP_PATH = 'src/content/clip.js'; // Task 1 Step 9 确认的产物路径

/** 抓取桩：点按钮注入 content script，结果经 onMessage 进 store。完整草案编辑在 Task 7。 */
export function ClipView() {
  const { pageContext, error } = usePageContext();

  async function handleCapture() {
    const [tab] = await chrome.tabs.query({ active: true, currentWindow: true });
    if (!tab.id) return;
    await chrome.scripting.executeScript({ target: { tabId: tab.id }, files: [CLIP_PATH] });
  }

  return (
    <div>
      <button className="btn btn-primary" onClick={handleCapture}>✂️ 抓取本页</button>
      <p className="msg msg-info">划词优先（先选中内容），否则自动提取正文 + 图片。</p>
      {error && <p className="msg msg-error">{error}</p>}
      {pageContext && <p className="msg msg-info">已抓取：{pageContext.title || pageContext.sourceUrl}</p>}
    </div>
  );
}
```

- [ ] **Step 8: 实现 `views/registry.ts`**

```ts
// extension/src/sidepanel/views/registry.ts
import type { SidePanelView } from './types.js';
import { ClipView } from './ClipView.js';
import { ChatView } from './ChatView.js';

/** 视图注册表（扩展点）：加功能 = push 一项，容器逻辑不动（设计 §3.5） */
export const VIEWS: SidePanelView[] = [
  { id: 'clip', title: '剪藏', icon: '✂️', component: ClipView },
  { id: 'chat', title: '对话', icon: '💬', component: ChatView },
];
```

- [ ] **Step 9: 实现 `SidePanel.tsx`（容器）**

```tsx
// extension/src/sidepanel/SidePanel.tsx
import { useEffect, useState } from 'react';
import { VIEWS } from './views/registry.js';

const TAB_KEY = 'atf-sidepanel-tab';

export function SidePanel() {
  const [activeId, setActiveId] = useState<string>(VIEWS[0].id);

  // 记忆当前 Tab（chrome.storage）
  useEffect(() => {
    chrome.storage?.local
      .get(TAB_KEY)
      .then((r) => {
        if (r[TAB_KEY]) setActiveId(r[TAB_KEY] as string);
      })
      .catch(() => {});
  }, []);

  function switchTab(id: string) {
    setActiveId(id);
    chrome.storage?.local.set({ [TAB_KEY]: id }).catch(() => {});
  }

  const Active = VIEWS.find((v) => v.id === activeId)?.component ?? VIEWS[0].component;

  return (
    <div className="sp-root">
      <nav className="sp-tabs">
        {VIEWS.map((v) => (
          <button
            key={v.id}
            className={`sp-tab${activeId === v.id ? ' active' : ''}`}
            onClick={() => switchTab(v.id)}
          >
            {v.icon} {v.title}
          </button>
        ))}
      </nav>
      <main className="sp-main">
        <Active />
      </main>
      <footer className="sp-footer">AI Task Flow 剪藏 · 调用 localhost:3000</footer>
    </div>
  );
}
```

- [ ] **Step 10: 实现 `styles.css`（原生 CSS + 设计 Token + 暗色）**

```css
/* extension/src/sidepanel/styles.css */
:root {
  --bg: #ffffff;
  --surface: #f7f7f8;
  --surface-2: #eef0f3;
  --text: #1f2328;
  --text-2: #57606a;
  --border: #d8dee4;
  --primary: #2563eb;
  --primary-weak: #dbeafe;
  --error: #dc2626;
}
@media (prefers-color-scheme: dark) {
  :root {
    --bg: #0d1117;
    --surface: #161b22;
    --surface-2: #21262d;
    --text: #e6edf3;
    --text-2: #8b949e;
    --border: #30363d;
    --primary-weak: #1e3a5f;
  }
}
* { box-sizing: border-box; }
body { margin: 0; font-family: system-ui, sans-serif; font-size: 13px; background: var(--bg); color: var(--text); }

.sp-root { display: flex; flex-direction: column; height: 100vh; }
.sp-tabs { display: flex; border-bottom: 1px solid var(--border); }
.sp-tab { flex: 1; padding: 8px 12px; font-size: 13px; background: none; border: none; cursor: pointer; color: var(--text-2); border-bottom: 2px solid transparent; }
.sp-tab.active { color: var(--text); font-weight: 600; border-bottom-color: var(--primary); }
.sp-main { flex: 1; overflow: auto; padding: 12px; }
.sp-footer { border-top: 1px solid var(--border); padding: 4px 12px; font-size: 11px; color: var(--text-2); }
.sp-empty { padding: 16px; text-align: center; font-size: 13px; color: var(--text-2); }
.sp-empty p { margin-top: 8px; font-size: 11px; opacity: 0.7; }

.btn { padding: 6px 12px; border-radius: 6px; border: 1px solid var(--border); background: var(--surface); color: var(--text); cursor: pointer; font-size: 13px; }
.btn-primary { background: var(--primary); color: #fff; border-color: var(--primary); }
.btn:disabled { opacity: 0.5; cursor: not-allowed; }
.input, .textarea { width: 100%; padding: 6px 8px; border-radius: 6px; border: 1px solid var(--border); background: var(--surface); color: var(--text); font-size: 13px; font-family: inherit; }
.textarea { resize: vertical; }
.row { display: flex; gap: 8px; align-items: center; margin: 8px 0; }
.row-between { display: flex; gap: 8px; align-items: center; justify-content: space-between; margin: 8px 0; }

.draft-card { border: 1px solid var(--border); border-radius: 8px; padding: 10px; margin-bottom: 10px; background: var(--surface); }
.draft-title { font-weight: 600; margin-bottom: 6px; }
.step { display: flex; gap: 8px; margin: 4px 0; align-items: flex-start; }
.step-body { flex: 1; }
.step img { max-height: 96px; max-width: 100%; border-radius: 4px; margin-top: 4px; display: block; }

.msg { padding: 6px 8px; border-radius: 6px; margin: 8px 0; font-size: 12px; }
.msg-info { background: var(--primary-weak); color: var(--primary); }
.msg-error { background: rgba(220, 38, 38, 0.12); color: var(--error); }
.muted { color: var(--text-2); font-size: 11px; }
```

- [ ] **Step 11: 实现 `main.tsx` + 改 `index.html`**

`extension/src/sidepanel/main.tsx`：
```tsx
import React from 'react';
import { createRoot } from 'react-dom/client';
import { SidePanel } from './SidePanel.js';
import { PageContextProvider } from './PageContextStore.js';
import './styles.css';

const rootEl = document.getElementById('root');
if (!rootEl) throw new Error('#root not found');

createRoot(rootEl).render(
  <React.StrictMode>
    <PageContextProvider>
      <SidePanel />
    </PageContextProvider>
  </React.StrictMode>,
);
```

`extension/src/sidepanel/index.html`（替换 Task 1 占位的 body）：
```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>AI Task Flow 剪藏</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="./main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 12: 实现 `background/index.ts`（点图标开侧栏）**

```ts
// extension/src/background/index.ts
// 点击工具栏图标 → 自动打开/聚焦侧栏（sidePanel 行为模式）。
// activeTab 权限由此触发，获得当前页临时抓取权限（不声明 <all_urls>，无高权限警告）。
chrome.runtime.onInstalled.addListener(() => {
  chrome.sidePanel
    .setPanelBehavior({ openPanelOnActionClick: true })
    .catch((error) => console.error('[ai-task-flow] setPanelBehavior 失败', error));
});
```

- [ ] **Step 13: 类型检查 + 构建**

Run：
```bash
cd extension && npx tsc --noEmit && npm run build
```
Expected：tsc 通过；`dist/` 含 background、sidepanel、clip 产物。

- [ ] **Step 14: 手动验证（侧栏容器 UI 需真实浏览器，无单测）**

Chrome「加载已解压的扩展」选 `extension/dist/` → 点扩展图标 → 侧栏打开 → 见「剪藏/对话」两 Tab + 抓取按钮 + 底部状态。**完整抓取→建任务链路留 Task 8 E2E。**

- [ ] **Step 15: Commit**
```bash
git add extension/src/background extension/src/sidepanel
git commit -m "feat(extension): 侧栏多视图宿主容器+PageContextStore+ChatView stub+抓取桩"
```

---

## Task 6: backend API client（captureToDrafts / createTaskFromDraft）

**Files:**
- Create: `extension/src/sidepanel/api/backend.ts`
- Create: `extension/src/sidepanel/api/__tests__/backend.test.ts`

- [ ] **Step 1: 先写失败测试 `backend.test.ts`**

```ts
// extension/src/sidepanel/api/__tests__/backend.test.ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { captureToDrafts, createTaskFromDraft } from '../backend.js';

beforeEach(() => {
  global.fetch = vi.fn() as unknown as typeof fetch;
});

describe('backend api', () => {
  it('captureToDrafts posts ClipRequest and returns drafts', async () => {
    (global.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
      ok: true,
      json: async () => ({ drafts: [{ title: 'T', description: 'D', steps: [] }], sourceUrl: 'u' }),
    });
    const resp = await captureToDrafts({
      sourceUrl: 'u',
      title: 't',
      text: '正文',
      images: [{ name: 'img-1', base64: 'data:x' }],
    });
    expect(resp.drafts).toHaveLength(1);
    const call = (global.fetch as ReturnType<typeof vi.fn>).mock.calls[0];
    expect(call[0]).toBe('http://localhost:3000/api/tasks/clip');
    const body = JSON.parse((call[1] as RequestInit).body as string);
    expect(body.sourceUrl).toBe('u');
    expect(body.content.text).toBe('正文');
    expect(body.images[0].name).toBe('img-1');
  });

  it('createTaskFromDraft posts web task with sourceUrl and prefix', async () => {
    (global.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
      ok: true,
      json: async () => ({ id: 'WEB-1' }),
    });
    await createTaskFromDraft({ title: 'T', description: 'D', steps: [] }, 'https://x.com/p', 'WEB');
    const call = (global.fetch as ReturnType<typeof vi.fn>).mock.calls[0];
    expect(call[0]).toBe('http://localhost:3000/api/tasks');
    const body = JSON.parse((call[1] as RequestInit).body as string);
    expect(body.source).toBe('web');
    expect(body.sourceUrl).toBe('https://x.com/p');
    expect(body.prefix).toBe('WEB');
  });

  it('throws on non-ok response with backend error', async () => {
    (global.fetch as ReturnType<typeof vi.fn>).mockResolvedValueOnce({
      ok: false,
      status: 400,
      json: async () => ({ error: '尚未配置 API Key' }),
    });
    await expect(
      createTaskFromDraft({ title: 'T', description: 'D', steps: [] }, 'u', 'WEB'),
    ).rejects.toThrow(/API Key/);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run：`cd extension && npx vitest run src/sidepanel/api/__tests__/backend.test.ts`
Expected：FAIL（模块不存在）。

- [ ] **Step 3: 实现 `backend.ts`**

```ts
// extension/src/sidepanel/api/backend.ts
import type { ClipRequest, ClipResponse, ClipDraft, CreateTaskRequest } from '@ai-task-flow/shared';
import type { PageContext } from '../../types/pageContext.js';

const BASE_URL = 'http://localhost:3000';

async function parseError(resp: Response): Promise<string> {
  const err = (await resp.json().catch(() => ({ error: resp.statusText }))) as { error?: string };
  return err.error ?? `请求失败(${resp.status})`;
}

/** PageContext → 后端拆解 → 任务草案（POST /api/tasks/clip，Plan 1 已就绪） */
export async function captureToDrafts(ctx: PageContext): Promise<ClipResponse> {
  const body: ClipRequest = {
    sourceUrl: ctx.sourceUrl,
    title: ctx.title,
    content: { text: ctx.text },
    images: ctx.images,
  };
  const resp = await fetch(`${BASE_URL}/api/tasks/clip`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(body),
  });
  if (!resp.ok) throw new Error(await parseError(resp));
  return (await resp.json()) as ClipResponse;
}

/** 草案 → 建任务（source='web' + sourceUrl + prefix，POST /api/tasks） */
export async function createTaskFromDraft(
  draft: ClipDraft,
  sourceUrl: string,
  prefix: string,
): Promise<void> {
  const body: CreateTaskRequest = {
    prefix,
    title: draft.title,
    description: draft.description,
    source: 'web',
    sourceUrl,
    steps: draft.steps,
  };
  const resp = await fetch(`${BASE_URL}/api/tasks`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(body),
  });
  if (!resp.ok) throw new Error(await parseError(resp));
}
```

- [ ] **Step 4: 跑测试确认通过**

Run：`cd extension && npx vitest run src/sidepanel/api/__tests__/backend.test.ts`
Expected：3 个测试 PASS。

- [ ] **Step 5: Commit**
```bash
git add extension/src/sidepanel/api
git commit -m "feat(extension): backend API client(拆解+建任务,source=web)"
```

---

## Task 7: ClipView 完整（抓取→草案预览→编辑→批量建任务）

**Files:**
- Modify: `extension/src/sidepanel/views/ClipView.tsx`（替换 Task 5 抓取桩）
- Create: `extension/src/sidepanel/components/DraftCard.tsx`

> 本 Task 是核心 UI，逻辑用 Task 6 的 api client（已单测），渲染靠 Task 8 手动 E2E 验证。

- [ ] **Step 1: 实现 `components/DraftCard.tsx`（单条草案编辑器）**

```tsx
// extension/src/sidepanel/components/DraftCard.tsx
import type { ClipDraft, TaskStep } from '@ai-task-flow/shared';

const BASE_URL = 'http://localhost:3000';

interface DraftCardProps {
  draft: ClipDraft;
  draftId: string;
  onChange: (next: ClipDraft) => void;
}

/** 单条任务草案编辑：标题、描述、勾选步骤、查看关联图。步骤/图文块在 MVP 不可拖拽重排。 */
export function DraftCard({ draft, draftId, onChange }: DraftCardProps) {
  function updateStep(index: number, next: TaskStep) {
    const steps = draft.steps.slice();
    steps[index] = next;
    onChange({ ...draft, steps });
  }

  return (
    <div className="draft-card">
      <input
        className="input draft-title"
        value={draft.title}
        placeholder="任务标题"
        onChange={(e) => onChange({ ...draft, title: e.target.value })}
      />
      <textarea
        className="textarea"
        rows={2}
        value={draft.description}
        placeholder="描述（可选）"
        onChange={(e) => onChange({ ...draft, description: e.target.value })}
      />
      {draft.steps.length > 0 && (
        <ol style={{ paddingLeft: 18, margin: '6px 0 0' }}>
          {draft.steps.map((step, si) => (
            <li key={`${draftId}-s${si}`} className="step">
              <input
                type="checkbox"
                checked={!!step.completed}
                onChange={(e) => updateStep(si, { ...step, completed: e.target.checked })}
              />
              <div className="step-body">
                {step.blocks?.map((b, bi) =>
                  b.type === 'text' ? (
                    <div key={`${draftId}-s${si}-b${bi}`}>{b.content}</div>
                  ) : (
                    <img key={`${draftId}-s${si}-b${bi}`} src={`${BASE_URL}${b.url}`} alt="截图" />
                  ),
                )}
              </div>
            </li>
          ))}
        </ol>
      )}
    </div>
  );
}
```

> **key 策略说明**：外层 drafts 列表用生成 id（见 ClipView `tagDrafts`）；内层步骤/图文块用 `${draftId}-s${si}` 组合键——MVP 不可重排，组合键稳定唯一，满足 React key 唯一性要求（重排功能上线后再换持久 id）。

- [ ] **Step 2: 实现 `ClipView.tsx`（完整）**

```tsx
// extension/src/sidepanel/views/ClipView.tsx
import { useEffect, useState } from 'react';
import type { ClipDraft } from '@ai-task-flow/shared';
import { usePageContext } from '../PageContextStore.js';
import { captureToDrafts, createTaskFromDraft } from '../api/backend.js';
import { DraftCard } from '../components/DraftCard.js';

const CLIP_PATH = 'src/content/clip.js'; // Task 1 Step 9 确认的产物路径

interface EditableDraft extends ClipDraft {
  id: string; // 渲染 key 用
}

type Busy = 'idle' | 'capturing' | 'creating';

export function ClipView() {
  const { pageContext, setPageContext, error } = usePageContext();
  const [drafts, setDrafts] = useState<EditableDraft[]>([]);
  const [prefix, setPrefix] = useState('WEB');
  const [busy, setBusy] = useState<Busy>('idle');
  const [msg, setMsg] = useState<{ kind: 'info' | 'error'; text: string } | null>(null);

  // 抓取按钮：注入 content script，结果经 onMessage 进 store（pageContext）
  async function handleCapture() {
    setMsg(null);
    setBusy('capturing');
    try {
      const [tab] = await chrome.tabs.query({ active: true, currentWindow: true });
      if (!tab.id) throw new Error('找不到当前标签页');
      await chrome.scripting.executeScript({ target: { tabId: tab.id }, files: [CLIP_PATH] });
      setMsg({ kind: 'info', text: '已发起抓取，等待页面返回…' });
    } catch (e) {
      setMsg({ kind: 'error', text: `抓取失败：${e instanceof Error ? e.message : String(e)}` });
    } finally {
      setBusy('idle');
    }
  }

  // pageContext 一更新就触发拆解（抓取结果异步经消息到达）
  useEffect(() => {
    if (!pageContext) return;
    let cancelled = false;
    setBusy('capturing');
    captureToDrafts(pageContext)
      .then((resp) => {
        if (cancelled) return;
        setDrafts(resp.drafts.map((d, i) => ({ ...d, id: `d${i}` })));
        setMsg({ kind: 'info', text: `拆解出 ${resp.drafts.length} 个任务草案` });
      })
      .catch((e) => {
        if (cancelled) return;
        setMsg({ kind: 'error', text: `拆解失败：${e instanceof Error ? e.message : String(e)}` });
      })
      .finally(() => {
        if (!cancelled) setBusy('idle');
      });
    return () => {
      cancelled = true;
    };
  }, [pageContext]);

  function updateDraft(index: number, next: ClipDraft) {
    const list = drafts.slice();
    list[index] = { ...next, id: list[index].id };
    setDrafts(list);
  }

  // 批量建任务：逐条 POST，部分失败继续，最后汇总
  async function handleCreate() {
    if (!pageContext || drafts.length === 0) return;
    setBusy('creating');
    setMsg(null);
    let ok = 0;
    for (const draft of drafts) {
      try {
        await createTaskFromDraft(draft, pageContext.sourceUrl, prefix);
        ok++;
      } catch (e) {
        setMsg({ kind: 'error', text: `建任务失败：${e instanceof Error ? e.message : String(e)}` });
      }
    }
    if (ok > 0) {
      setMsg({ kind: 'info', text: `已创建 ${ok}/${drafts.length} 个任务，看板将自动刷新` });
    }
    setBusy('idle');
    if (ok === drafts.length) {
      setDrafts([]);
      setPageContext(null);
    }
  }

  return (
    <div>
      <div className="row-between">
        <button className="btn btn-primary" onClick={handleCapture} disabled={busy !== 'idle'}>
          {busy === 'capturing' ? '抓取中…' : '✂️ 抓取本页'}
        </button>
        {drafts.length > 0 && (
          <button className="btn btn-primary" onClick={handleCreate} disabled={busy !== 'idle'}>
            {busy === 'creating' ? '创建中…' : `建任务（${drafts.length}）`}
          </button>
        )}
      </div>
      <p className="muted">划词优先（先选中内容再抓），否则自动提取正文+图片。</p>

      {drafts.length > 0 && (
        <div className="row">
          <span className="muted">任务前缀：</span>
          <input className="input" value={prefix} onChange={(e) => setPrefix(e.target.value)} style={{ width: 80 }} />
        </div>
      )}

      {msg && <p className={`msg msg-${msg.kind}`}>{msg.text}</p>}
      {error && <p className="msg msg-error">{error}</p>}

      {drafts.map((draft, i) => (
        <DraftCard key={draft.id} draft={draft} draftId={draft.id} onChange={(next) => updateDraft(i, next)} />
      ))}
    </div>
  );
}
```

- [ ] **Step 3: 类型检查 + 构建**

Run：
```bash
cd extension && npx tsc --noEmit && npm run build
```
Expected：通过；`dist/` 产出更新。

- [ ] **Step 4: 全量单测回归**

Run：`cd extension && npm test`
Expected：capture + images + PageContextStore + backend 全部 PASS（7 个测试）。

- [ ] **Step 5: Commit**
```bash
git add extension/src/sidepanel/views/ClipView.tsx extension/src/sidepanel/components
git commit -m "feat(extension): ClipView 草案预览+编辑+批量建任务"
```

---

## Task 8: 构建产出验证 + 手动 E2E

**Files:** 无新增/修改（验证 Task）

- [ ] **Step 1: 干净构建**

Run：`cd extension && npm run build`
Expected：`dist/` 完整，含 `manifest.json` / background / sidepanel / `src/content/clip.js`（或 Task 1 记录的 `$CLIP_PATH`）。

- [ ] **Step 2: 宿主后端启动 + 配 LLM**

另开终端：`cd backend && npm run dev`（监听 :3000）。
在「设置」配好 LLM API Key（剪藏依赖 LLM 拆解）。

- [ ] **Step 3: 加载扩展**

Chrome/Edge → `chrome://extensions` → 开「开发者模式」→「加载已解压的扩展程序」→ 选 `extension/dist/`。

- [ ] **Step 4: 抓取样例页 E2E**

打开任意含内容的网页（如一个缺陷页或博客）：
1. （可选）鼠标选中一段内容 + 截图区域。
2. 点工具栏扩展图标 → 侧栏打开。
3. 点「✂️ 抓取本页」→ 等 LLM 拆解 → 侧栏出现任务草案（标题/描述/步骤/图）。
4. 编辑标题/描述，勾选/查看步骤。
5. 点「建任务」→ 提示「已创建 N 个任务」。

- [ ] **Step 5: 看板验证**

打开 AI Task Flow 看板（`localhost:3000`）→ SSE 自动刷新 → 见新任务（🌐 来源角标显网页域名）→ 点开详情见「网页地址」（web 来源不显项目路径，验证 Plan 1 条件渲染）。顶部「来源筛选」选「网页剪藏」只显该任务。

- [ ] **Step 6: 异常路径验证（设计 §6）**

- 后端未启动时抓取 → 侧栏报「无法连接…」类错误（拆解失败提示）。
- LLM 未配 → 后端返回 400「API Key」→ 侧栏提示。
- 抓取空内容 → 拆解回退单任务（Plan 1 WebClipService 回退逻辑）。

- [ ] **Step 7: 回归全量单测**

Run（项目根）：`cd extension && npm test && cd ../backend && npm test -- --run`
Expected：扩展 7 测试 + 后端全量 PASS，无回归。

- [ ] **Step 8: Commit（如有 E2E 中发现的小修）**
```bash
# 仅当 E2E 触发了小修才提交；否则跳过
git add -A
git commit -m "fix(extension): E2E 调整(按实际)"
```

---

## 自审（Self-Review）

**1. Spec coverage（对照设计文档 `20260616203000`）：**
- §3.1 扩展 `extension/` MV3 + manifest（sidePanel/activeTab/scripting，host 仅 localhost）→ Task 1 ✅
- §3.1 `capturePageContext` 划词优先/否则 Readability + 图片 fetch→base64 → Task 2/3 ✅
- §3.1 background（开/聚焦侧栏）+ content 动态注入 → Task 4/5 ✅
- §3.1 SidePanel 容器（Tab + 视图路由 + 状态）→ Task 5 ✅
- §3.1 ClipView（抓取→预览→编辑→建任务）→ Task 5（桩）+ Task 7（完整）✅
- §3.5 多视图宿主 + registry 扩展点 + ChatView 预留 → Task 5 ✅
- §3.5 共享 PageContext（剪藏/对话共用）→ Task 2（类型）+ Task 5（Store）✅
- §4.1 POST /api/tasks/clip 契约 → Task 6（client）+ Plan 1（后端已就绪）✅
- §4.2 草案确认 → POST /api/tasks（source=web+sourceUrl+steps）→ Task 6/7 ✅
- §5 拆解 prompt + 图片按名引用（manifest）→ Task 2（manifest 末尾）+ Plan 1（prompt）✅
- §6 错误处理（后端不可达/LLM 失败回退/CORS 跳过/抓取空）→ Task 3（CORS）+ Task 7（错误提示）+ Task 8 Step 6（E2E）+ Plan 1（回退）✅
- §7 测试（capture/images 用 jsdom，E2E 手动+curl）→ Task 2/3/5/6 单测 + Task 8 E2E ✅
- §8 工程组织（monorepo extension/ + shared 类型 + vite/crxjs）→ Task 1 ✅

**2. Placeholder scan：** 无 TBD/TODO；每个 code step 给了完整代码块；UI/background/content 入口的手动验证已诚实标注（无单测），非占位。

**3. Type consistency：**
- `PageContext { sourceUrl, title, text, images? }`（Task 2 定义）在 capture（Task 2）、store（Task 5）、api client（Task 6，转 ClipRequest）、ClipView（Task 7）一致。✅
- `collectImages(root): Promise<ClipImage[]>`（Task 3 定义）在 capture.ts（Task 2）调用一致；命名 `img-N` 在 images 测试、capture manifest、clipPrompt（Plan 1）一致。✅
- 消息类型 `CAPTURE_RESULT` / `CAPTURE_ERROR`（Task 4 发送、Task 5 监听）字符串一致。✅
- `CLIP_PATH = 'src/content/clip.js'`（Task 5/7 用）与 Task 1 Step 9 确认路径对齐（已注明若不同则同步改）。✅
- `captureToDrafts(ctx)` / `createTaskFromDraft(draft, sourceUrl, prefix)`（Task 6 定义）在 Task 7 调用签名一致。✅
- `SidePanelView { id, title, icon, component }`（Task 5 types）在 registry、SidePanel 一致。✅
- `EditableDraft = ClipDraft & { id }`（Task 7）与 DraftCard props（draftId）一致。✅

**潜在风险/执行注意：**
- **vite 版本**：crxjs 2.5.0 在 vite > 5.0.11 构建失败（issue #874），package.json 已锁 `~5.0.11`。若 npm 强装了更高小版本，手动降到 5.0.11。
- **content 产物路径**：crxjs 输出 `src/content/clip.js`（保留源前缀），Task 1 Step 9 已要求核对 `$CLIP_PATH`；若不同，同步改 Task 5/7 的 `CLIP_PATH` 常量。
- **Readability 在 jsdom**：对极简 article 可能返回 null，capture.ts 已用 `?? document.body.textContent` 回退；Task 2 测试若偶发 FAIL，给测试 article 补长正文。
- **prefix 必填**：后端 `POST /api/tasks` 的 `TaskId.create(prefix,...)` 要求 prefix 非空，ClipView 默认 `'WEB'`（用户可改），与设计「剪藏任务留空项目信息后入库」兼容——任务以 TODO 入库，用户看板补项目信息再派发。
- **shared 仅类型耦合**：扩展全用 `import type`，构建不含 shared 运行时；执行前确保 `shared/dist` 存在（`npm run build:shared`）。
- **图片绝对路径**：草案图 url 为 `/api/uploads/xxx`（相对），侧栏（扩展源）渲染需前缀 `http://localhost:3000`（DraftCard 已加）。
- **手动验证占比**：UI/background/content 入口无单测，靠 Task 8 E2E 兜底（与 Plan 1 Task 10/11 手动 UI 验证一致的处理）。

---

## 完成后

- 浏览器扩展可一键剪藏任意网页 → AI 拆解 → 侧栏确认 → 看板出现 🌐 来源任务，与手动任务（📁）通过来源区分展示与筛选。
- 侧栏已是多视图宿主：后续做对话视图（设计 §3.5）= 在 `registry.ts` 把 ChatView stub 换成真实组件 + 复用看板 `/api/chat`，容器不动。
- 端到端闭环：**网页 → 扩展采集 → 后端 LLM 拆解 → 草案确认 → 建任务 → 看板 SSE 刷新** 全通。
```
