# 网页剪藏 - 宿主应用改造 实施计划（Plan 1）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 AI Task Flow 后端能接收网页剪藏请求（`POST /api/tasks/clip`）并用 LLM 拆解成任务草案，同时给 `Task` 加 `source`/`sourceUrl` 字段区分任务来源（web/manual），看板详情页/卡片按来源条件渲染地址、支持来源筛选。

**Architecture:** 复用现有 `Task.steps`（图文块）模型，仅新增 `source: 'web'|'manual'` + `sourceUrl?` 两个领域字段。后端新增 `WebClipService`（存 base64 图 → 调 `LlmProvider.generateObject` 结构化拆解 → 产出 `TaskStep[]` 草案，失败回退单任务）。前端零新增组件，仅 `TaskDrawer`（地址按来源条件渲染）、`TaskCard`（来源角标）、`App.tsx`/`BoardToolbar`（来源筛选）改展示逻辑。浏览器扩展本身（Plan 2）不在本计划内。

**Tech Stack:** Node.js + TypeScript + Fastify（后端）、React + Zustand + Tailwind（前端）、`@ai-task-flow/shared`（共享类型）、vitest（测试）、tsyringe（DI，仅注册，路由为函数式注入）。

**关联设计文档:** `docs/20260616203000_网页内容剪藏插件设计.md`（§3.2 后端 / §3.4 来源区分）。

---

## 文件结构总览

**新增（Create）：**
| 文件 | 职责 |
|------|------|
| `backend/src/infrastructure/storage/saveBase64Image.ts` | base64 data URL → 落盘 `uploads/` → 返回 `/api/uploads/xxx.ext` |
| `backend/src/application/webclip/WebClipService.ts` | 应用层：存图 + LLM 拆解 + 回退，产出 `ClipDraft[]` |
| `backend/src/application/webclip/clipPrompt.ts` | 通用自适应拆解 prompt（设计 §5） |
| `backend/src/interfaces/http/routes/webClipRoutes.ts` | `POST /api/tasks/clip` 路由 |
| `backend/src/infrastructure/storage/__tests__/saveBase64Image.test.ts` | 工具单测 |
| `backend/src/application/webclip/__tests__/WebClipService.test.ts` | 服务单测（mock LLM） |
| `backend/src/interfaces/http/__tests__/webClipRoutes.test.ts`（并入 server.test.ts 或新建） | 路由集成测试 |

**修改（Modify）：**
| 文件 | 改动 |
|------|------|
| `shared/src/types/task.ts` | 加 `TaskSource` 类型；`TaskDTO`/`CreateTaskRequest`/`UpdateTaskRequest` 加 `source`/`sourceUrl` |
| `shared/src/types/webclip.ts`（新） | `ClipRequest`/`ClipDraft`/`ClipResponse`/`ClipImage` |
| `shared/src/index.ts` | `export * from './types/webclip.js'` |
| `backend/src/domain/workflow/entities/Task.ts` | 构造函数末尾加 `source`/`sourceUrl`（可选带默认）；`toJSON`/`applyUpdate` 加字段 |
| `backend/src/infrastructure/persistence/JsonTaskRepository.ts` | `dtoToEntity` 读取 `dto.source`（兼容旧数据默认 `manual`） |
| `backend/src/interfaces/http/routes/taskRoutes.ts` | `POST /api/tasks` 读 `source`/`sourceUrl`；`GET /api/tasks` 加 `?source=` 过滤 |
| `backend/src/interfaces/http/server.ts` | `createHttpServer`/`startHttpServer` 增加 `webClipService` 参数并注册路由 |
| `backend/src/http-server.ts` | `startApp` 实例化 `WebClipService` 并传入 |
| `backend/src/interfaces/http/__tests__/server.test.ts` | beforeEach 构造 `WebClipService` 传入 |
| `frontend/src/components/TaskCard.tsx` | 加来源角标（🌐 web / 📁 manual） |
| `frontend/src/components/TaskDrawer.tsx` | EMPTY_TASK 加 `source`；地址区按 `source` 条件渲染（web 显 sourceUrl 只读 / manual 显项目路径） |
| `frontend/src/stores/uiStore.ts` | 加 `sourceFilter` + setter |
| `frontend/src/components/BoardToolbar.tsx` | 加来源筛选 Select |
| `frontend/src/App.tsx` | `filteredTasks` 加 `sourceFilter` 过滤 |

---

## 关键设计约定（贯穿所有 Task，避免不一致）

1. **`TaskSource`** = `'web' | 'manual'`，定义在 `shared/src/types/task.ts`。
2. **`Task` 构造函数** 在末尾追加两个可选参数（**带默认值**，避免破坏现有 8 处 `new Task(...)` 调用）：
   ```ts
   constructor(
     ..., createdAt: Date = new Date(), updatedAt: Date = new Date(),
     public source: TaskSource = 'manual',
     public sourceUrl?: string,
   ) {}
   ```
3. **旧数据兼容**：`JsonTaskRepository.dtoToEntity` 读取时 `dto.source ?? 'manual'`（历史任务无此字段 → 视为手动）。
4. **`sourceUrl` 仅 web 来源有值**；manual 来源 `sourceUrl` 为 `undefined`。
5. 所有测试用 vitest，命名 `should_<预期>_when_<条件>`，一个测试一个行为。

---

## Task 1: shared 类型扩展（source / sourceUrl）

**Files:**
- Modify: `shared/src/types/task.ts`
- Test: 无独立测试（类型变更，由 Task 2/3 的运行测试间接验证）；本任务结尾跑 `npm run build` 确认编译通过。

- [ ] **Step 1: 在 `shared/src/types/task.ts` 顶部（`Priority` 枚举后）加类型**

```ts
/** 任务来源：web = 浏览器插件剪藏；manual = 看板手动新建 */
export type TaskSource = 'web' | 'manual';
```

- [ ] **Step 2: 给 `TaskDTO` 加字段（在 `relatedFiles` 后、`steps` 前，或紧邻 `repoPath`）**

在 `TaskDTO` 接口的 `projectName?: string;` 后面加：
```ts
  source: TaskSource;            // 任务来源(web/manual)
  sourceUrl?: string;            // 仅 web 来源:抓取页面的 URL
```

- [ ] **Step 3: 给 `CreateTaskRequest` 加字段（在 `projectName?: string;` 后）**
```ts
  source?: TaskSource;
  sourceUrl?: string;
```

- [ ] **Step 4: 给 `UpdateTaskRequest` 加同样字段（在 `projectName?: string;` 后）**
```ts
  source?: TaskSource;
  sourceUrl?: string;
```

- [ ] **Step 5: 构建验证**

Run: `cd shared && npm run build`
Expected: 编译通过，产出 `shared/dist`。

- [ ] **Step 6: Commit**
```bash
git add shared/src/types/task.ts
git commit -m "feat(shared): Task 加 source/sourceUrl 区分来源(web/manual)"
```

---

## Task 2: Task 领域模型加 source / sourceUrl

**Files:**
- Modify: `backend/src/domain/workflow/entities/Task.ts`
- Modify: `backend/src/domain/workflow/entities/__tests__/Task.test.ts`（新增 2 个测试）

- [ ] **Step 1: 先写失败测试**

在 `Task.test.ts` 末尾 `describe` 块内追加：
```ts
  it('should default source to manual when not provided', () => {
    const task = new Task(
      TaskId.create('WS', 10), 't', 'd',
      TaskStatus.TODO, Priority.P1, undefined, undefined, [], [],
    );
    expect(task.source).toBe('manual');
    expect(task.sourceUrl).toBeUndefined();
  });

  it('should persist source/sourceUrl in toJSON and applyUpdate', () => {
    const task = new Task(
      TaskId.create('WS', 11), 't', 'd',
      TaskStatus.TODO, Priority.P1, undefined, undefined, [], [],
      undefined, undefined, new Date(), new Date(),
      'web', 'https://example.com/bug/1',
    );
    expect(task.source).toBe('web');
    expect(task.sourceUrl).toBe('https://example.com/bug/1');
    expect(task.toJSON().source).toBe('web');
    expect(task.toJSON().sourceUrl).toBe('https://example.com/bug/1');

    task.applyUpdate({ sourceUrl: 'https://example.com/bug/2' });
    expect(task.sourceUrl).toBe('https://example.com/bug/2');
  });
```
顶部 import 补充（若缺）：`import type { TaskSource } from '@ai-task-flow/shared';`（测试里用的是字面量，可不导入类型，但 import 备用）。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && npx vitest run src/domain/workflow/entities/__tests__/Task.test.ts`
Expected: 两个新测试 FAIL（`task.source` 是 `undefined`，不等于 `'manual'`）。

- [ ] **Step 3: 修改 `Task.ts` 构造函数**

文件顶部 import 区加：
```ts
import type { TaskSource } from '@ai-task-flow/shared';
```

把构造函数签名末尾改为（在 `updatedAt: Date = new Date(),` 后追加两行）：
```ts
  constructor(
    public readonly id: TaskId,
    public title: string,
    public description: string,
    public status: TaskStatus,
    public priority: Priority,
    public repoPath: string | undefined,
    public projectName: string | undefined,
    public relatedFiles: string[],
    public steps: TaskStep[],
    public worktree?: WorktreeRef,
    public executionResult?: ExecutionResult,
    public createdAt: Date = new Date(),
    public updatedAt: Date = new Date(),
    public source: TaskSource = 'manual',
    public sourceUrl?: string,
  ) {}
```

- [ ] **Step 4: 修改 `toJSON()`（在 `relatedFiles` 行后、`steps` 行前插入）**

```ts
  toJSON() {
    return {
      id: this.id.value,
      title: this.title,
      description: this.description,
      status: this.status,
      priority: this.priority,
      repoPath: this.repoPath,
      projectName: this.projectName,
      relatedFiles: this.relatedFiles,
      source: this.source,
      sourceUrl: this.sourceUrl,
      steps: this.steps,
      // ... worktree / executionResult / createdAt / updatedAt 保持不变
```

- [ ] **Step 5: 修改 `applyUpdate`（类型 + 赋值）**

`applyUpdate` 的 `updates` 参数类型加两行：
```ts
  applyUpdate(updates: {
    title?: string;
    description?: string;
    status?: TaskStatus;
    priority?: Priority;
    repoPath?: string;
    projectName?: string;
    relatedFiles?: string[];
    steps?: TaskStep[];
    source?: TaskSource;
    sourceUrl?: string;
  }): void {
```
方法体内（`if (updates.steps !== undefined)...` 后）加：
```ts
    if (updates.source !== undefined) this.source = updates.source;
    if (updates.sourceUrl !== undefined) this.sourceUrl = updates.sourceUrl;
```

- [ ] **Step 6: 跑测试确认通过**

Run: `cd backend && npx vitest run src/domain/workflow/entities/__tests__/Task.test.ts`
Expected: 全部 PASS（含 2 个新测试 + 原有 9 个）。

- [ ] **Step 7: Commit**
```bash
git add backend/src/domain/workflow/entities/Task.ts backend/src/domain/workflow/entities/__tests__/Task.test.ts
git commit -m "feat(domain): Task 聚合根支持 source/sourceUrl"
```

---

## Task 3: JsonTaskRepository 序列化兼容 source

**Files:**
- Modify: `backend/src/infrastructure/persistence/JsonTaskRepository.ts`
- Modify: `backend/src/infrastructure/persistence/__tests__/JsonTaskRepository.test.ts`（新增 1 测试）

- [ ] **Step 1: 先写失败测试**

在 `JsonTaskRepository.test.ts` 的 describe 块末尾加：
```ts
  it('should round-trip source/sourceUrl and default legacy tasks to manual', async () => {
    const webTask = new Task(
      TaskId.create('TEST', 5), 'Web task', 'desc',
      TaskStatus.TODO, Priority.P1, undefined, undefined, [], [],
      undefined, undefined, new Date(), new Date(),
      'web', 'https://example.com/page',
    );
    await repository.save(webTask);
    const found = await repository.findById(webTask.id);
    expect(found?.source).toBe('web');
    expect(found?.sourceUrl).toBe('https://example.com/page');
  });
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && npx vitest run src/infrastructure/persistence/__tests__/JsonTaskRepository.test.ts`
Expected: FAIL（`found?.source` 为 `undefined`）。

- [ ] **Step 3: 修改 `dtoToEntity`**

在 `JsonTaskRepository.ts` 的 `dtoToEntity` 方法，`new Task(...)` 调用末尾（`new Date(dto.updatedAt)` 之后）追加两个参数：
```ts
    return new Task(
      TaskId.fromString(dto.id),
      dto.title,
      dto.description,
      dto.status,
      dto.priority,
      dto.repoPath,
      dto.projectName,
      dto.relatedFiles,
      normalizeSteps(dto.steps),
      worktree,
      executionResult,
      new Date(dto.createdAt),
      new Date(dto.updatedAt),
      dto.source ?? 'manual',   // 旧数据无 source 字段 → 视为手动
      dto.sourceUrl,
    );
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd backend && npx vitest run src/infrastructure/persistence/__tests__/JsonTaskRepository.test.ts`
Expected: 全部 PASS。

- [ ] **Step 5: Commit**
```bash
git add backend/src/infrastructure/persistence/JsonTaskRepository.ts backend/src/infrastructure/persistence/__tests__/JsonTaskRepository.test.ts
git commit -m "feat(persistence): JsonTaskRepository 往返 source/sourceUrl"
```

---

## Task 4: taskRoutes — POST 传 source + GET ?source= 筛选

**Files:**
- Modify: `backend/src/interfaces/http/routes/taskRoutes.ts`
- Modify: `backend/src/interfaces/http/__tests__/server.test.ts`（新增 2 测试）

- [ ] **Step 1: 先写失败测试**

在 `server.test.ts` 的 `describe('Task CRUD')` 块末尾加：
```ts
    it('should create web-sourced task with sourceUrl', async () => {
      const response = await server.inject({
        method: 'POST',
        url: '/api/tasks',
        payload: {
          prefix: 'WS', title: 'Clipped', description: 'from web',
          source: 'web', sourceUrl: 'https://example.com/bug/1',
        },
      });
      expect(response.statusCode).toBe(201);
      const body = JSON.parse(response.body);
      expect(body.source).toBe('web');
      expect(body.sourceUrl).toBe('https://example.com/bug/1');
    });

    it('should filter tasks by source via query', async () => {
      // 建 1 个 web + 1 个 manual
      await server.inject({ method: 'POST', url: '/api/tasks', payload: {
        prefix: 'WS', title: 'Web one', description: 'd', source: 'web', sourceUrl: 'u1',
      }});
      await server.inject({ method: 'POST', url: '/api/tasks', payload: {
        prefix: 'WS', title: 'Manual one', description: 'd',
      }});

      const webResp = await server.inject({ method: 'GET', url: '/api/tasks?source=web' });
      const manualResp = await server.inject({ method: 'GET', url: '/api/tasks?source=manual' });

      const webTasks = JSON.parse(webResp.body);
      const manualTasks = JSON.parse(manualResp.body);
      expect(webTasks).toHaveLength(1);
      expect(webTasks[0].source).toBe('web');
      expect(manualTasks).toHaveLength(1);
      expect(manualTasks[0].source).toBe('manual');
    });
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && npx vitest run src/interfaces/http/__tests__/server.test.ts`
Expected: 两个新测试 FAIL（`body.source` undefined / 筛选返回全部）。

- [ ] **Step 3: 修改 `GET /api/tasks` 加 query 过滤**

把 taskRoutes.ts 的 `GET /api/tasks` handler 改为：
```ts
  // GET /api/tasks - 获取所有任务(可选 ?source=web|manual 过滤)
  fastify.get<{ Querystring: { source?: string } }>('/api/tasks', async (request) => {
    const tasks = await taskRepository.findAll();
    const { source } = request.query;
    const filtered = source ? tasks.filter((t) => t.source === source) : tasks;
    return filtered.map(toDTO);
  });
```

- [ ] **Step 4: 修改 `POST /api/tasks` 读 source/sourceUrl**

把 POST handler 的解构与 `new Task(...)` 改为：
```ts
  fastify.post<{ Body: CreateTaskRequest }>('/api/tasks', async (request, reply) => {
    const { prefix, title, description, priority, repoPath, projectName, relatedFiles, steps, source, sourceUrl } = request.body;

    const allTasks = await taskRepository.findAll();
    const maxSeq = allTasks
      .filter(t => t.id.prefix === prefix)
      .map(t => t.id.sequence)
      .reduce((max, seq) => Math.max(max, seq), 0);

    const task = new Task(
      TaskId.create(prefix, maxSeq + 1),
      title,
      description,
      TaskStatus.TODO,
      priority || Priority.P1,
      repoPath,
      projectName,
      relatedFiles || [],
      steps || [],
      undefined,        // worktree
      undefined,        // executionResult
      new Date(),       // createdAt
      new Date(),       // updatedAt
      source ?? 'manual',
      sourceUrl,
    );

    await taskRepository.save(task);
    try { await writeTaskDoc(task); } catch (error: unknown) {
      const message = error instanceof Error ? error.message : String(error);
      fastify.log.warn(`Failed to write task doc for ${task.id.value}: ${message}`);
    }
    return reply.status(201).send(toDTO(task));
  });
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd backend && npx vitest run src/interfaces/http/__tests__/server.test.ts`
Expected: 全部 PASS。

- [ ] **Step 6: Commit**
```bash
git add backend/src/interfaces/http/routes/taskRoutes.ts backend/src/interfaces/http/__tests__/server.test.ts
git commit -m "feat(http): POST 建任务支持 source;GET /api/tasks 支持 ?source= 筛选"
```

---

## Task 5: base64 图片保存工具

**Files:**
- Create: `backend/src/infrastructure/storage/saveBase64Image.ts`
- Create: `backend/src/infrastructure/storage/__tests__/saveBase64Image.test.ts`

- [ ] **Step 1: 先写失败测试**

```ts
// backend/src/infrastructure/storage/__tests__/saveBase64Image.test.ts
import 'reflect-metadata';
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import fs from 'fs/promises';
import path from 'path';
import os from 'os';
import { saveBase64Image } from '../saveBase64Image.js';

describe('saveBase64Image', () => {
  let uploadsDir: string;

  beforeEach(() => {
    // 用临时目录覆盖环境,避免污染真实 uploads
    uploadsDir = path.join(os.tmpdir(), `test-uploads-${Date.now()}`);
    process.env.AI_TASK_FLOW_DATA_DIR = path.dirname(uploadsDir);
    // saveBase64Image 内部用 uploadsDirPath(),其默认 {dataDir}/uploads
    // 为精确控制,这里把 dataDir 设为临时根,uploads 即在其下
  });

  afterEach(async () => {
    try { await fs.rm(path.dirname(uploadsDir), { recursive: true, force: true }); } catch {}
    delete process.env.AI_TASK_FLOW_DATA_DIR;
  });

  it('should save png base64 and return /api/uploads url', async () => {
    // 1x1 透明 png 的 base64
    const dataUrl = 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNk+M8AAAMBAQDJ/pLvAAAAAElFTkSuQmCC';
    const url = await saveBase64Image(dataUrl, uploadsDir);
    expect(url).toMatch(/^\/api\/uploads\/[a-f0-9]+\.png$/);
    const filename = url.replace('/api/uploads/', '');
    const stat = await fs.stat(path.join(uploadsDir, filename));
    expect(stat.size).toBeGreaterThan(0);
  });

  it('should throw on invalid data url', async () => {
    await expect(saveBase64Image('not-a-data-url', uploadsDir)).rejects.toThrow(/Invalid base64/);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && npx vitest run src/infrastructure/storage/__tests__/saveBase64Image.test.ts`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 `saveBase64Image.ts`**

```ts
// backend/src/infrastructure/storage/saveBase64Image.ts
import fs from 'fs/promises';
import path from 'path';
import crypto from 'crypto';
import { uploadsDirPath } from '../../config/dataDir.js';

const MIME_TO_EXT: Record<string, string> = {
  'image/png': 'png',
  'image/jpeg': 'jpg',
  'image/gif': 'gif',
  'image/webp': 'webp',
};

/**
 * 把 base64 data URL(如 data:image/png;base64,...)落盘到 uploads 目录,
 * 返回可被前端/Claude 访问的相对路径 /api/uploads/<hash>.<ext>。
 *
 * @param dataUrl  data:image/<type>;base64,<payload>
 * @param customDir 可选自定义目录(测试用),默认 uploadsDirPath()
 */
export async function saveBase64Image(dataUrl: string, customDir?: string): Promise<string> {
  const match = /^data:(image\/[a-z+]+);base64,(.+)$/i.exec(dataUrl);
  if (!match) {
    throw new Error('Invalid base64 image data URL');
  }
  const mime = match[1].toLowerCase();
  const ext = MIME_TO_EXT[mime] ?? 'png';
  const buffer = Buffer.from(match[2], 'base64');

  const dir = customDir ?? uploadsDirPath();
  await fs.mkdir(dir, { recursive: true });
  const filename = `${crypto.randomBytes(16).toString('hex')}.${ext}`;
  await fs.writeFile(path.join(dir, filename), buffer);

  return `/api/uploads/${filename}`;
}
```

> 注：测试里通过 `customDir` 参数显式传目录，避免依赖 `AI_TASK_FLOW_DATA_DIR`。若 `uploadsDirPath()` 已读取该环境变量则两路径径一致；以 `customDir` 为准。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd backend && npx vitest run src/infrastructure/storage/__tests__/saveBase64Image.test.ts`
Expected: 2 个测试 PASS。

- [ ] **Step 5: Commit**
```bash
git add backend/src/infrastructure/storage/
git commit -m "feat(storage): saveBase64Image 工具(data URL→uploads→/api/uploads url)"
```

---

## Task 6: shared webclip 类型 + 拆解 prompt

**Files:**
- Create: `shared/src/types/webclip.ts`
- Modify: `shared/src/index.ts`
- Create: `backend/src/application/webclip/clipPrompt.ts`

- [ ] **Step 1: 创建 `shared/src/types/webclip.ts`**

```ts
// shared/src/types/webclip.ts
import type { TaskStep } from './task.js';

/** 抓取到的图片(base64 形式,WebClipService 落盘后转为 url) */
export interface ClipImage {
  name: string;        // 占位引用名,如 "img-1"
  base64: string;      // data:image/png;base64,...
}

/** 扩展 → 后端的剪藏请求 */
export interface ClipRequest {
  sourceUrl: string;                    // location.href
  title: string;                        // document.title
  content: { html?: string; text: string };
  images?: ClipImage[];
}

/** AI 拆解出的单个任务草案 */
export interface ClipDraft {
  title: string;
  description: string;
  steps: TaskStep[];   // 图文块(image block 的 url 已替换为真实地址)
}

/** 后端 → 扩展的响应 */
export interface ClipResponse {
  drafts: ClipDraft[];
  sourceUrl: string;
}
```

- [ ] **Step 2: 在 `shared/src/index.ts` 加导出**

```ts
export * from './types/webclip.js';
```

- [ ] **Step 3: 构建 shared 确认类型可用**

Run: `cd shared && npm run build`
Expected: 通过。

- [ ] **Step 4: 创建 `backend/src/application/webclip/clipPrompt.ts`**

```ts
// backend/src/application/webclip/clipPrompt.ts

/**
 * 通用网页内容拆解 prompt(设计文档 §5)。
 * 不预设内容类型,让 LLM 按语义边界自适应拆分。
 */
export const CLIP_SYSTEM_PROMPT = `你是任务拆解助手。用户会给你一段从网页抓取的内容(可能含图片占位)。
请按语义边界把它拆成若干个独立、可执行的任务。

拆分规则(自适应,不预设内容类型):
- 若内容含步骤/地址/编号结构(如"1、2、3"或"步骤1"),按步骤拆;
- 若是列表,按列表项拆;
- 若是连续段落且各自独立主题,按主题拆;
- 若整体是一个主题,就返回 1 个任务(不要硬拆)。

关键:同一页面(sourceUrl)的多个问题点归到"同一任务的多个 steps";
只有不同页面才拆成不同任务(通常只产出 1 个任务,含多个步骤)。

每个任务输出:title、description(整体说明,可含页面位置)、steps(步骤数组)。
steps 内的 image block 用 {type:"image",ref:"img-1"} 引用图片占位,由后端替换为真实 url。
只输出 JSON,不要任何解释文字。`;

export function buildClipUserPrompt(pageText: string): string {
  return `----- 网页内容 -----\n${pageText}\n\n----- 输出格式 -----\n只输出 JSON: {"drafts":[{"title":"","description":"","steps":[{"blocks":[{"type":"text","content":""},{"type":"image","ref":"img-1"}]}]}]}`;
}

/** LLM 输出的原始草案结构(image 用 ref,待替换) */
export interface RawClipDraft {
  title: string;
  description: string;
  steps: { blocks?: { type: 'text'; content: string } | { type: 'image'; ref: string }[] }[];
}
```

- [ ] **Step 5: Commit**
```bash
git add shared/src/types/webclip.ts shared/src/index.ts backend/src/application/webclip/clipPrompt.ts
git commit -m "feat(webclip): shared 契约类型 + 通用拆解 prompt"
```

---

## Task 7: WebClipService（存图 + LLM 拆解 + 回退）

**Files:**
- Create: `backend/src/application/webclip/WebClipService.ts`
- Create: `backend/src/application/webclip/__tests__/WebClipService.test.ts`

- [ ] **Step 1: 先写失败测试（mock LLM）**

```ts
// backend/src/application/webclip/__tests__/WebClipService.test.ts
import 'reflect-metadata';
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import fs from 'fs/promises';
import path from 'path';
import os from 'os';
import { WebClipService } from '../WebClipService.js';
import type { LlmProvider, StreamChunk } from '../../../infrastructure/llm/LlmProvider.js';
import type { LlmConfigService } from '../../llm-config/LlmConfigService.js';

let testUploadsDir: string;

beforeEach(() => { testUploadsDir = path.join(os.tmpdir(), `clip-uploads-${Date.now()}`); });
afterEach(async () => { try { await fs.rm(testUploadsDir, { recursive: true, force: true }); } catch {} });

/** 构造一个返回固定 generateObject 结果的 mock provider */
function mockProvider(result: unknown): LlmProvider {
  return {
    async *streamText(): AsyncIterable<StreamChunk> { yield { delta: '', done: true }; },
    async generateObject(): Promise<unknown> { return result; },
  };
}

/** 构造 mock LlmConfigService:返回指定 provider,且 isConfigured 可控 */
function mockLlmConfig(provider: LlmProvider, configured = true): LlmConfigService {
  return {
    getProvider: () => provider,
    isConfigured: () => configured,
  } as unknown as LlmConfigService;
}

describe('WebClipService', () => {
  it('should decompose content into drafts and replace image refs with urls', async () => {
    const llmResult = {
      drafts: [{
        title: '审核页未国际化',
        description: '页面位置:审核判定页',
        steps: [{ blocks: [
          { type: 'text', content: '提示文本未国际化' },
          { type: 'image', ref: 'img-1' },
        ]}],
      }],
    };
    const service = new WebClipService(mockLlmConfig(mockProvider(llmResult)), testUploadsDir);

    const resp = await service.clip({
      sourceUrl: 'https://x.com/bug/1',
      title: 'Bug',
      content: { text: '审核判定页文本未国际化' },
      images: [{ name: 'img-1', base64: 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNk+M8AAAMBAQDJ/pLvAAAAAElFTkSuQmCC' }],
    });

    expect(resp.sourceUrl).toBe('https://x.com/bug/1');
    expect(resp.drafts).toHaveLength(1);
    expect(resp.drafts[0].title).toBe('审核页未国际化');
    const imgBlock = resp.drafts[0].steps[0].blocks!.find((b) => b.type === 'image');
    expect(imgBlock && imgBlock.type === 'image' && imgBlock.url).toMatch(/^\/api\/uploads\//);
  });

  it('should fallback to a single whole-task draft when LLM decompose throws', async () => {
    const badProvider: LlmProvider = {
      async *streamText(): AsyncIterable<StreamChunk> { yield { delta: '', done: true }; },
      async generateObject(): Promise<unknown> { throw new Error('boom'); },
    };
    const service = new WebClipService(mockLlmConfig(badProvider), testUploadsDir);

    const resp = await service.clip({
      sourceUrl: 'https://x.com/bug/2',
      title: 'Title',
      content: { text: '一段内容' },
      images: [{ name: 'img-1', base64: 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNk+M8AAAMBAQDJ/pLvAAAAAElFTkSuQmCC' }],
    });

    expect(resp.drafts).toHaveLength(1);
    // 回退:所有图作为单任务步骤
    const blocks = resp.drafts[0].steps[0].blocks!;
    expect(blocks.some((b) => b.type === 'image')).toBe(true);
  });

  it('should throw friendly error when LLM not configured', async () => {
    const service = new WebClipService(mockLlmConfig(mockProvider({ drafts: [] }), false), testUploadsDir);
    await expect(service.clip({
      sourceUrl: 'u', title: 't', content: { text: 'x' },
    })).rejects.toThrow(/API Key/);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && npx vitest run src/application/webclip/__tests__/WebClipService.test.ts`
Expected: FAIL（`WebClipService` 不存在）。

- [ ] **Step 3: 实现 `WebClipService.ts`**

```ts
// backend/src/application/webclip/WebClipService.ts
import type { LlmConfigService } from '../llm-config/LlmConfigService.js';
import type { LlmMessage } from '../../infrastructure/llm/LlmProvider.js';
import { saveBase64Image } from '../../infrastructure/storage/saveBase64Image.js';
import { FileLogger } from '../../infrastructure/logging/FileLogger.js';
import { CLIP_SYSTEM_PROMPT, buildClipUserPrompt } from './clipPrompt.js';
import type { ClipRequest, ClipDraft, ClipResponse } from '@ai-task-flow/shared';
import type { StepBlock, TaskStep } from '@ai-task-flow/shared';

const logger = new FileLogger('webclip');

/** LLM 原始输出(图片用 ref) */
interface RawOutput { drafts: { title: string; description: string; steps: { blocks?: any[] }[] }[]; }

export class WebClipService {
  constructor(
    private readonly llmConfigService: LlmConfigService,
    private readonly uploadsDir?: string,   // 测试注入
  ) {}

  async clip(request: ClipRequest): Promise<ClipResponse> {
    if (!this.llmConfigService.isConfigured()) {
      throw new Error('尚未配置 API Key,请先在「设置」配置 LLM 后再剪藏');
    }

    // 1. 落盘图片:name → /api/uploads/xxx url
    const imageMap = new Map<string, string>();
    for (const img of request.images ?? []) {
      try {
        const url = await saveBase64Image(img.base64, this.uploadsDir);
        imageMap.set(img.name, url);
      } catch (error: unknown) {
        // 单张图失败不阻断,跳过
        logger.warn('保存图片失败,跳过', { name: img.name, error: String(error) });
      }
    }

    // 2. LLM 拆解,失败回退单任务
    let drafts: ClipDraft[];
    try {
      drafts = await this.decompose(request.content.text, imageMap);
    } catch (error: unknown) {
      logger.warn('LLM 拆解失败,回退为单任务', { error: String(error) });
      drafts = [this.fallbackDraft(request, imageMap)];
    }

    return { drafts, sourceUrl: request.sourceUrl };
  }

  private async decompose(text: string, imageMap: Map<string, string>): Promise<ClipDraft[]> {
    const llm = this.llmConfigService.getProvider();
    const messages: LlmMessage[] = [
      { role: 'system', content: CLIP_SYSTEM_PROMPT },
      { role: 'user', content: buildClipUserPrompt(text) },
    ];
    const raw = await llm.generateObject<RawOutput>(messages, {});
    if (!raw?.drafts?.length) {
      throw new Error('LLM 未返回有效草案');
    }
    return raw.drafts.map((d) => this.resolveDraft(d, imageMap));
  }

  /** 把 LLM 输出的 ref 占位替换为真实图片 url */
  private resolveDraft(raw: RawOutput['drafts'][number], imageMap: Map<string, string>): ClipDraft {
    const steps: TaskStep[] = (raw.steps ?? []).map((s) => {
      const blocks: StepBlock[] = (s.blocks ?? []).flatMap((b) => {
        if (b.type === 'text') {
          return b.content ? [{ type: 'text' as const, content: b.content }] : [];
        }
        if (b.type === 'image') {
          const url = imageMap.get(b.ref);
          return url ? [{ type: 'image' as const, url }] : [];
        }
        return [];
      });
      return { blocks };
    });
    return { title: raw.title, description: raw.description, steps };
  }

  /** 回退:原文进 description,所有图作为单任务步骤 */
  private fallbackDraft(request: ClipRequest, imageMap: Map<string, string>): ClipDraft {
    const imageBlocks: StepBlock[] = [...imageMap.values()].map((url) => ({ type: 'image', url }));
    const textBlock: StepBlock[] = request.content.text.trim()
      ? [{ type: 'text', content: request.content.text.slice(0, 1000) }]
      : [];
    const blocks = [...textBlock, ...imageBlocks];
    return {
      title: request.title || '网页剪藏任务',
      description: request.content.text.slice(0, 500),
      steps: blocks.length > 0 ? [{ blocks }] : [{ blocks: [{ type: 'text', content: '（无内容）' }] }],
    };
  }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd backend && npx vitest run src/application/webclip/__tests__/WebClipService.test.ts`
Expected: 3 个测试 PASS。

- [ ] **Step 5: Commit**
```bash
git add backend/src/application/webclip/WebClipService.ts backend/src/application/webclip/__tests__/WebClipService.test.ts
git commit -m "feat(webclip): WebClipService 存图+LLM拆解+回退"
```

---

## Task 8: POST /api/tasks/clip 路由 + 接入 server

**Files:**
- Create: `backend/src/interfaces/http/routes/webClipRoutes.ts`
- Modify: `backend/src/interfaces/http/server.ts`
- Modify: `backend/src/http-server.ts`
- Modify: `backend/src/interfaces/http/__tests__/server.test.ts`（新增 1 集成测试）

- [ ] **Step 1: 先写失败测试**

在 `server.test.ts` 顶部 beforeEach 构造处加 `webClipService`，并新增 describe：
```ts
// beforeEach 内,在 chatService 构造后加:
import { WebClipService } from '../../../application/webclip/WebClipService.js';
// ...
const webClipService = new WebClipService(llmConfigService);

// 把 webClipService 传入 createHttpServer(需先改 server.ts 签名,见 Step 4)
```
在文件底部 `describe('HTTP Server')` 内加：
```ts
  describe('Web Clip', () => {
    it('should return drafts from POST /api/tasks/clip', async () => {
      // 注意:此集成测试依赖真实 LLM 会变 flaky。这里用空图片+无 LLM 配置,
      // 预期走 isConfigured 拦截返回 400,验证路由已挂载。
      const resp = await server.inject({
        method: 'POST',
        url: '/api/tasks/clip',
        payload: { sourceUrl: 'u', title: 't', content: { text: 'x' } },
      });
      // 未配置 LLM 时应 400 且消息含 API Key
      expect([400, 200]).toContain(resp.statusCode);
    });
  });
```
> 真实拆解路径由 Task 7 的 mock 单测覆盖；此集成测试只验证路由挂载与参数流转，避免依赖外部 LLM。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd backend && npx vitest run src/interfaces/http/__tests__/server.test.ts`
Expected: FAIL（`createHttpServer` 签名不匹配 / 路由不存在）。

- [ ] **Step 3: 创建 `webClipRoutes.ts`**

```ts
// backend/src/interfaces/http/routes/webClipRoutes.ts
import { FastifyInstance } from 'fastify';
import type { WebClipService } from '../../../application/webclip/WebClipService.js';
import type { ClipRequest } from '@ai-task-flow/shared';

export async function registerWebClipRoutes(
  fastify: FastifyInstance,
  webClipService: WebClipService,
) {
  // POST /api/tasks/clip - 抓取内容 → AI 拆解 → 任务草案(不落库,等前端确认)
  fastify.post<{ Body: ClipRequest }>('/api/tasks/clip', async (request, reply) => {
    const { sourceUrl, title, content, images } = request.body;
    if (!sourceUrl || !content?.text) {
      return reply.status(400).send({ error: 'sourceUrl 和 content.text 必填' });
    }
    try {
      const result = await webClipService.clip({ sourceUrl, title, content, images });
      return reply.send(result);
    } catch (error: unknown) {
      const message = error instanceof Error ? error.message : String(error);
      // 未配置 LLM 等业务错误 → 400;其余 500
      const status = /API Key/.test(message) ? 400 : 500;
      return reply.status(status).send({ error: message });
    }
  });
}
```

- [ ] **Step 4: 修改 `server.ts` 签名 + 注册路由**

import 区加：
```ts
import type { WebClipService } from '../../application/webclip/WebClipService.js';
import { registerWebClipRoutes } from './routes/webClipRoutes.js';
```
`createHttpServer` 和 `startHttpServer` 的参数列表，在 `llmConfigService: LlmConfigService,` 后加 `webClipService: WebClipService,`。
在 `createHttpServer` 内路由注册区（`registerLlmConfigRoutes` 之后）加：
```ts
  await registerWebClipRoutes(fastify, webClipService);
```
`startHttpServer` 内调用 `createHttpServer(...)` 时把 `webClipService` 透传。

- [ ] **Step 5: 修改 `http-server.ts` 实例化并传入**

import 区加：
```ts
import { WebClipService } from './application/webclip/WebClipService.js';
```
在 `startApp` 内（`chatService` 构造后）加：
```ts
  const webClipService = new WebClipService(llmConfigService);
```
把 `return startHttpServer(config, taskRepository, eventBus, worktreeManager, chatRepository, chatService, llmConfigService, webClipService);` 末尾补 `webClipService`。

- [ ] **Step 6: 更新 `server.test.ts` beforeEach 透传 webClipService**

在 beforeEach 的 `createHttpServer(...)` 调用末尾补 `, webClipService`（其中 `const webClipService = new WebClipService(llmConfigService);` 已在 Step 1 加）。

- [ ] **Step 7: 跑测试确认通过**

Run: `cd backend && npx vitest run src/interfaces/http/__tests__/server.test.ts`
Expected: 全部 PASS（含新 Web Clip 测试）。

- [ ] **Step 8: 全量回归**

Run: `cd backend && npm test -- --run`
Expected: 全部测试 PASS（52 + 新增）。

- [ ] **Step 9: Commit**
```bash
git add backend/src/interfaces/http/routes/webClipRoutes.ts backend/src/interfaces/http/server.ts backend/src/http-server.ts backend/src/interfaces/http/__tests__/server.test.ts
git commit -m "feat(http): POST /api/tasks/clip 路由 + WebClipService 接入"
```

---

## Task 9: 前端 TaskCard 来源角标

**Files:**
- Modify: `frontend/src/components/TaskCard.tsx`

- [ ] **Step 1: 在 TaskCard 标题行旁加来源角标**

在 `TaskCard.tsx` 的 `<h4>` 标题块（约 L112）下方、`{task.description && ...}` 之前，插入来源角标：
```tsx
      {/* 来源角标:web 显网页域名,manual 显项目名 */}
      <div className="mb-1.5 flex items-center gap-1 text-xs" style={{ color: 'var(--text-3)' }}>
        {task.source === 'web' ? (
          <span className="inline-flex items-center gap-1" title={task.sourceUrl}>
            🌐 {task.sourceUrl ? new URL(task.sourceUrl).hostname : '网页'}
          </span>
        ) : (
          task.projectName && <span>📁 {task.projectName}</span>
        )}
      </div>
```

> 说明：web 来源用 `new URL(sourceUrl).hostname` 显示域名（如 `bug.xfyun.cn`），可点击跳转的需求留到 Task 10 详情页。`new URL` 解析失败时回退显示"网页"字样，避免崩溃：
```tsx
        {task.source === 'web' ? (
          <span className="inline-flex items-center gap-1" title={task.sourceUrl}>
            🌐 {safeHostname(task.sourceUrl)}
          </span>
        ) : (
```
在组件内（`TaskCard` 函数体内，`canDispatch` 附近）加辅助函数：
```tsx
  function safeHostname(url?: string): string {
    try { return url ? new URL(url).hostname : '网页'; } catch { return '网页'; }
  }
```

- [ ] **Step 2: 前端类型检查**

Run: `cd frontend && npx tsc --noEmit`
Expected: 通过（`task.source` / `task.sourceUrl` 来自已扩展的 shared `TaskDTO`）。

- [ ] **Step 3: Commit**
```bash
git add frontend/src/components/TaskCard.tsx
git commit -m "feat(frontend): TaskCard 来源角标(🌐 web / 📁 manual)"
```

---

## Task 10: 前端 TaskDrawer 详情按来源条件渲染地址

**Files:**
- Modify: `frontend/src/components/TaskDrawer.tsx`

- [ ] **Step 1: EMPTY_TASK 模板加 source 默认值**

把 `EMPTY_TASK` 常量（约 L100）改为：
```ts
const EMPTY_TASK: TaskDTO = {
  id: '',
  title: '',
  description: '',
  status: TaskStatus.TODO,
  priority: Priority.P1,
  repoPath: undefined,
  projectName: undefined,
  relatedFiles: [],
  steps: [],
  source: 'manual',   // 看板手动新建,默认 manual
  createdAt: '',
  updatedAt: '',
};
```

- [ ] **Step 2: 替换"项目路径"Section 为按来源条件渲染**

找到现有"项目路径(可选)"Section（约 L349-385，`<Section label="项目路径(可选)">...</Section>` 整块），替换为：
```tsx
          {/* 地址区:按来源条件渲染——web 显网页地址(只读可点击),manual 显项目路径(可编辑) */}
          {task.source === 'web' ? (
            <Section label="网页地址">
              <div className="text-sm break-all" style={{ color: 'var(--text-2)' }}>
                {task.sourceUrl ? (
                  <a
                    href={task.sourceUrl}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="underline hover:opacity-80"
                    style={{ color: 'var(--primary-6)' }}
                  >
                    🌐 {task.sourceUrl}
                  </a>
                ) : (
                  <span style={{ color: 'var(--text-3)' }}>(无网页地址)</span>
                )}
              </div>
            </Section>
          ) : (
            <Section label="项目路径(可选)">
              <div className="flex gap-2">
                <Input
                  value={repoPath}
                  onChange={(e) => setRepoPath(e.target.value)}
                  onBlur={() => checkProjectPath()}
                  placeholder="点「浏览」选择,或直接粘贴路径"
                />
                <button
                  type="button"
                  onClick={async () => {
                    try {
                      const { path } = await systemApi.selectDirectory();
                      if (path) {
                        setRepoPath(path);
                        await checkProjectPath();
                      }
                    } catch {
                      toast.error('打开文件夹选择器失败');
                    }
                  }}
                  className="inline-flex shrink-0 items-center gap-1 whitespace-nowrap rounded px-3 py-2 text-sm transition-fast hover:opacity-80"
                  style={{ backgroundColor: 'var(--surface-2)', color: 'var(--text-2)' }}
                  title="打开系统文件选择器"
                >
                  <FolderOpen size={14} />
                  浏览…
                </button>
                {pathValid === true && <span className="self-center text-green-600">✓</span>}
                {pathValid === false && <span className="self-center text-red-600">✗</span>}
              </div>
              {projectName && (
                <div className="mt-1 text-xs" style={{ color: 'var(--text-3)' }}>
                  项目名: {projectName}
                </div>
              )}
            </Section>
          )}
```
> 隐藏的文件夹选择器 input（原 L388-424）保留原位不动——它只对 manual 来源的浏览按钮生效，web 来源不触发。

- [ ] **Step 3: markdown 预览同步区分来源**

找到 markdown useMemo（约 L252-276），把 `if (projectName) {...} if (repoPath) {...}` 段改为：
```ts
    if (task.source === 'web') {
      lines.push(`**来源**: 网页剪藏`);
      if (task.sourceUrl) lines.push(`**网页地址**: ${task.sourceUrl}`);
    } else {
      if (projectName) lines.push(`**项目**: ${projectName}`);
      if (repoPath) lines.push(`**仓库路径**: \`${repoPath}\``);
    }
```

- [ ] **Step 4: 类型检查**

Run: `cd frontend && npx tsc --noEmit`
Expected: 通过。

- [ ] **Step 5: 手动验证（启动前端，打开一个 manual 任务详情 → 见项目路径；构造一个 web 任务见网页地址）**

Run: `cd frontend && npm run dev`（浏览器肉眼确认；web 任务可用 curl 造数据）
造 web 任务：后端启动后 `curl -X POST localhost:3000/api/tasks -H "Content-Type: application/json" -d '{"prefix":"WS","title":"测试web","description":"d","source":"web","sourceUrl":"https://example.com/p"}'`

- [ ] **Step 6: Commit**
```bash
git add frontend/src/components/TaskDrawer.tsx
git commit -m "feat(frontend): TaskDrawer 地址区按来源条件渲染(web 网页地址/manual 项目路径)"
```

---

## Task 11: 前端来源筛选

**Files:**
- Modify: `frontend/src/stores/uiStore.ts`
- Modify: `frontend/src/components/BoardToolbar.tsx`
- Modify: `frontend/src/App.tsx`

- [ ] **Step 1: uiStore 加 sourceFilter**

在 `uiStore.ts` 的 `UIState` 接口加（`projectFilter` 旁）：
```ts
  sourceFilter: 'web' | 'manual' | null;
  setSourceFilter: (s: 'web' | 'manual' | null) => void;
```
初始 state 加：`sourceFilter: null,`
实现加（`setProjectFilter` 旁）：
```ts
  setSourceFilter: (source) => set({ sourceFilter: source }),
```

- [ ] **Step 2: BoardToolbar 加来源 Select**

import 加：`import { useTaskStore } from '@/stores/taskStore';`（如需统计有无 web 任务，可选）。在 `BoardToolbar.tsx` 的项目 Select（约 L55-64）后、`<div className="ml-auto...">` 前，插入来源 Select：
```tsx
      <div className="w-32">
        <Select
          value={sourceFilter ?? ''}
          onChange={(e) => setSourceFilter((e.target.value || null) as 'web' | 'manual' | null)}
          options={[
            { label: '全部来源', value: '' },
            { label: '🌐 网页剪藏', value: 'web' },
            { label: '📁 手动新建', value: 'manual' },
          ]}
        />
      </div>
```
组件顶部加 store 绑定：
```tsx
  const sourceFilter = useUIStore((s) => s.sourceFilter);
  const setSourceFilter = useUIStore((s) => s.setSourceFilter);
```

- [ ] **Step 3: App.tsx filteredTasks 加 sourceFilter 过滤**

在 `App.tsx` 顶部加 `const sourceFilter = useUIStore((s) => s.sourceFilter);`（与 `projectFilter` 同处，约 L34）。把 `filteredTasks` useMemo（约 L79-84）改为：
```tsx
  const filteredTasks = useMemo(() => {
    return tasks.filter((t) => {
      if (projectFilter && t.projectName !== projectFilter) return false;
      if (sourceFilter && t.source !== sourceFilter) return false;
      if (searchQuery) {
        const q = searchQuery.toLowerCase();
        if (!t.title.toLowerCase().includes(q) && !t.id.toLowerCase().includes(q)) return false;
      }
      return true;
    });
  }, [tasks, searchQuery, projectFilter, sourceFilter]);
```
> 若现有 searchQuery 分支与上面写法不同，保留原有 search 逻辑，只在 `projectFilter` 判断后加 `sourceFilter` 一行 + 依赖数组加 `sourceFilter`。

- [ ] **Step 4: 类型检查 + 手动验证**

Run: `cd frontend && npx tsc --noEmit`
Expected: 通过。
启动前端，切换"全部来源 / 网页剪藏 / 手动新建"，确认列表正确过滤。

- [ ] **Step 5: Commit**
```bash
git add frontend/src/stores/uiStore.ts frontend/src/components/BoardToolbar.tsx frontend/src/App.tsx
git commit -m "feat(frontend): 看板来源筛选(全部/网页剪藏/手动新建)"
```

---

## 自审（Self-Review）

**1. Spec coverage（对照设计文档）：**
- §3.2 后端 `POST /api/tasks/clip` + WebClipService + 拆解 prompt → Task 6/7/8 ✅
- §3.2 领域模型新增 `source`/`sourceUrl` → Task 1/2/3 ✅
- §3.4 `GET /api/tasks?source=` 筛选 → Task 4 ✅
- §3.4 详情页按来源条件渲染地址（web 显 sourceUrl / manual 显 repoPath）→ Task 10 ✅
- §3.4 卡片角标（🌐 web / 📁 manual）→ Task 9 ✅
- §3.4 来源筛选条 → Task 11 ✅
- §3.3 看板复用 steps 展示、不新增组件 → 贯穿（Task 9/10/11 仅改展示）✅
- 复用 `/api/upload`（base64 变体）→ Task 5 ✅
- §5 拆解 prompt + 回退单任务 → Task 6/7 ✅

**2. Placeholder scan：** 无 TBD/TODO；每个 code step 都给了完整代码块。

**3. Type consistency：**
- `TaskSource = 'web' | 'manual'`（Task 1 定义）在 Task 2（Task 构造/applyUpdate）、Task 3（repo）、Task 4（route query）、Task 9/10/11（前端）一致使用。✅
- `source`/`sourceUrl` 字段名贯穿 shared → domain → repo → route → frontend，无别名漂移。✅
- `WebClipService` 构造 `(llmConfigService, uploadsDir?)` 在 Task 7 定义、Task 8 接入、测试 mock 三处一致。✅
- `ClipRequest`/`ClipDraft`/`ClipResponse`（Task 6 定义）在 Task 7 service、Task 8 route 一致。✅
- `saveBase64Image(dataUrl, customDir?)` 签名在 Task 5 定义、Task 7 调用一致。✅

**潜在风险/执行注意：**
- Task 8 的 `server.test.ts` Web Clip 集成测试因不 mock LLM，只验证路由挂载（未配置时返回 400），真实拆解由 Task 7 mock 单测覆盖——已在测试注释说明，避免执行者误以为是断言拆解结果。
- Task 5 测试用 `customDir` 注入临时目录，避免依赖 `AI_TASK_FLOW_DATA_DIR` 环境变量；实现已支持该参数。
- shared 类型变更后若前端构建报缺导出，检查 `frontend/node_modules/@ai-task-flow/shared` 是否为陈旧物理拷贝（CHANGELOG v0.2.0 已记录此坑），删除后回退 workspace symlink。

---

## 完成后

后端 `POST /api/tasks/clip` 可用 curl 测试（需先在「设置」配好 LLM）：
```bash
curl -X POST http://localhost:3000/api/tasks/clip \
  -H "Content-Type: application/json" \
  -d '{"sourceUrl":"https://example.com/bug/1","title":"测试","content":{"text":"1、审核页文本未国际化 2、列表页排版错乱"},"images":[]}'
```
预期返回 `{drafts:[...], sourceUrl}`。本计划完成后即可进入 **Plan 2（浏览器扩展）**——扩展调此接口。
