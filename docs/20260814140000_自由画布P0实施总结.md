# 自由画布 P0 实施总结与问题记录

> 实施时间：2026-08-15
> 分支：`feat/free-canvas`
> 提交：`ca16eea`（P0a 前端核心）+ `69f5c4b`（P0b 后端基座）
> 依据：`20260814140000_自由画布功能设计.md` v3 第十一章 P0a/P0b 清单

---

## 一、交付内容

### P0a：前端自由画布核心（16 文件，+1158 行）

| 文件 | 类型 | 内容 |
|------|------|------|
| `shared/src/types/mindmap.ts` | 改 | `MindmapNodeData` 加 `imageUrl/href/style`；`CanvasNodeStyle` + `CanvasFill`（5 语义 + 5 chart）；edge `data.label` |
| `floatingEdgeUtils.ts` | 新 | 浮边几何算法（中心连线与包围盒交点），纯函数 + measured 空值兜底（13 单测） |
| `BranchEdge.tsx` | 改 | 浮边渲染（nodeLookup 精确订阅）+ 连线标签 `EdgeLabelRenderer` |
| `FloatingConnectionLine.tsx` | 新 | 拖拽临时连线（自写，走同一浮边算法，虚线预览） |
| `MindmapNode.tsx` | 改 | 单 source handle（Loose）+ 换行 max-w-280 + textarea 多行编辑 + `data-style` 透传 + 自动进入编辑 + 空内容失焦删除 |
| `autoEditQueue.ts` | 新 | 双击建节点自动编辑信号（模块级，不落库） |
| `useCanvasActions.ts` | 新 | `createTextAt` / `deleteSelection`（单事务）+ `isTreeDocument` 模式判定（9 单测） |
| `useAlignmentSnap.ts` | 新 | 对齐吸附算法（6 参考线 + 8px/zoom 阈值，官方 helper-lines 拦截模式，本地 state）（7 单测） |
| `HelperLines.tsx` | 新 | 辅助线 SVG 覆盖层（store transform 精确订阅换算屏幕坐标） |
| `MindmapEditor.tsx` | 改 | `ConnectionMode.Loose` + `selectionOnDrag` 框选 + `deleteKeyCode={null}` 自管删除 + 自环/重复边拦截 + 双击建节点（pane 过滤）+ 连线双击加标签（浮层输入框）+ 网格开关 + 拖拽快照时序修正 |
| `canvasContextMenu.ts` | 改 | 加"显示/隐藏网格"菜单项 |
| `index.css` | 改 | `data-style` 语义 tint（5 语义色 + chart-1~5，color-mix oklch）+ `.mm-helperline` |

### P0b：后端基座（7 文件，+238/-88）

| 文件 | 类型 | 内容 |
|------|------|------|
| `shared/src/utils/treeLayout.ts` | 新 | DFS 布局抽到 shared（前端/后端/MCP 共用） |
| `frontend/.../mindmap/layout.ts` | 改 | 重导出 shared（import 路径兼容，5 测试全过） |
| `migrations.ts` | 新 | `MIGRATIONS` 数组 + `applyMigrations`（v0→v1），幂等，未知版本容忍（5 单测） |
| `JsonMindmapRepository.ts` | 改 | 读时迁移 + 写时补 `schemaVersion` |
| `tests/curl/test_mindmap_crud.sh` | 改 | 修 Windows 中文 400 + 新增画布字段 round-trip 用例 |

### 验证结果

- 前端：typecheck ✅ / vitest **126 passed + 3 skipped** / vite build ✅
- 后端：typecheck ✅ / vitest **373 passed**（46 文件）
- e2e：curl **13/13 通过**（含 image/link 节点 + style + 连线标签持久化、`schemaVersion:1` 落盘）

---

## 二、实施决策记录（偏离设计的部分）

### D1. 不新建 CanvasService/canvasRoutes（P0b-3 收窄）

设计文档 P0b 清单含"CanvasService CRUD + canvasRoutes HTTP CRUD"。实施时发现：P0b 已决定**留在单文件 `mindmaps.json` + 同一聚合根**，现有 `MindmapService`/`mindmapRoutes` 已提供完整 CRUD + DomainError 四件套 + 乐观锁——再建平行模块是纯重复。改为：扩展现有 curl 测试覆盖画布新字段。真正的画布新后端能力（语义摘要/MCP 工具/zip 导出）按计划属 P1b，届时落地 CanvasService。

### D2. 浮边算法自写而非照抄官方

官方 FloatingEdges 的 `getNodeIntersection` 用对方节点尺寸算中心有已知瑕疵（假设两节点同尺寸）。自写版本改为正确的射线-矩形求交（`t = min(tx, ty)`），语义更准且可测。单方向求交只依赖自身 measured（对方未测时退化为其左上角，与官方一致）。

### D3. RF 版本差异适配

项目 `@xyflow/react` 12.11.3 的 store **无 `connectionStartHandle`**（新版才有），拖拽连线起始节点改用 `s.connection.fromNode`（`connection.inProgress` 时）。

### D4. 拖拽撤销快照时序修正（顺手修的存量 bug）

原 `onNodeDragStop` 拍快照——拍的是**拖后**状态，该撤销步无效（撤销回到拖后=无变化）。修正为 `onNodeDragStart` 拍（变更前），符合 useUndoRedo 的"变更前快照"契约。

### D5. 模式判定用启发式而非 docMode 字段

`isTreeDocument`（存在无入边节点 + 所有节点入度≤1）区分树形/画布操作语义。**局限**：恰好构成树形的自由画布会走树形语义（Tab/Enter/删子树）。P1 若引入 `docMode` 字段可消除歧义。

### D6. Delete 语义双轨 + RF 内置删除禁用

`deleteKeyCode={null}` 禁用 RF 内置删除，统一由 `handleKeyDown` 分流：树形=删子树（原行为），画布=删选中节点+选中边（单事务单快照）。

---

## 三、遗留问题（待用户决策/后续处理）

| # | 问题 | 影响 | 建议 |
|---|------|------|------|
| Q1 | **键盘焦点丢失**：节点 textarea 失焦后焦点回 body，Tab/Enter/Delete 失效直到重新点画布 | 双击建节点→输入→失焦后无法立即 Delete 删它 | 节点 onBlur 后画布容器 refocus（设计文档九章已提，P1a 补） |
| Q2 | **tree 模式下 `panOnDrag=true` 与画布一致**，但 `zoomOnDoubleClick` 在树形文档保留双击放大——双击节点编辑已 stopPropagation，不冲突 | 无 | 已验证逻辑正确，无需改 |
| Q3 | **辅助线只在单节点/联合盒层面吸附**，多选拖动时是对联合盒对齐（非逐节点） | 多选拖动时个体节点间不对齐 | 可接受（Figma 同为整体对齐），P2 视反馈再议 |
| Q4 | **连线标签输入框**是 fixed 定位浮层（点击处+8px），非跟随连线中点 | 视觉上标签编辑位置在鼠标处，提交后标签渲染在贝塞尔中点 | 可接受；P1a 若做样式面板可顺手优化 |
| Q5 | **双 React 测试问题**：根目录 vitest 跑前端组件测试仍有 27 失败（P1-14 存量），但 `frontend/` 内 vitest 全绿 | CI 需用 frontend 的 vitest（现状已如此） | 存量问题，专项处理（P1-14） |
| Q6 | **`snapToGrid` 未启用**（设计为 P1 联动网格开关），当前网格纯视觉 | 无吸附网格 | P1a 接入 |
| Q7 | **image/link 节点类型已定义但组件未实现**（P1a），当前打开含此类节点的文档该节点不渲染（nodeTypes 未注册） | 若 curl 测试数据混入正式库会有不可见节点 | P1a 首个任务补 ImageNode/LinkNode |
| Q8 | **画布模式无 Tab/Enter 建节点快捷键**（双击是唯一入口） | 键盘党不便 | P1a 可加 Tab=在选中节点旁建节点 |
| Q9 | e2e 的 schemaVersion 检查依赖固定数据目录路径（非 /tmp/mm-canvas-test 时跳过） | 换环境该断言不生效 | 可接受（单测已覆盖迁移逻辑） |

---

## 四、验收对照（设计文档 P0a/P0b 验收标准）

| 验收项 | 状态 |
|--------|------|
| 双击建节点（自动编辑/空删） | ✅ 代码+单测（真机待验） |
| 4 方向拖线（Loose+浮边） | ✅ 代码+13 单测 |
| 对齐线（8px/zoom） | ✅ 代码+7 单测 |
| 框选 + Delete 删选中 | ✅ 代码（deleteSelection 单事务） |
| 换行 max-w-280 | ✅ |
| CRUD curl + 迁移幂等 + schemaVersion | ✅ 13/13 e2e |
| **真机浏览器验证** | ⏳ 未做（需用户或下轮 web-gui-tester） |

> P0 验收标准中"真机走查"未执行——本环境无法起浏览器实测，建议下轮用 web-gui-tester 技能对双击/拖线/对齐线做 GUI 验证。
