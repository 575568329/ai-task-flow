node_modules/
dist/
.ai-workspaces/
*.log
.logs/
.env
.DS_Store

# TS build artifacts
*.tsbuildinfo
dist-tsnode/

# 用户上传的图片(运行时产物,不进版本库)
backend/uploads/
.env.local

# 前端 build 产物(vite build 输出到此;npm publish 时由 backend 的 prepublishOnly
# 自动 `cd .. && npm run build` 现建,npm pack 经 files:["public"] 打包带走,不入版本库)
backend/public/

# Claude Code 本地个人权限设置(每个开发者/每台机器不同,不进版本库)
.claude/settings.local.json

# pnpm 锁文件(本项目统一用 npm,忽略 pnpm 误生成的锁文件)
pnpm-lock.yaml

# 看板程序(kanban-code)误把绝对路径当文件名写进项目目录的 hook-events 日志,反复出现
*hook-events.jsonl
