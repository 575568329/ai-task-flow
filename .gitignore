node_modules/
dist/
.ai-workspaces/
*.log
.logs/
.env
.DS_Store

# TS build artifacts
*.tsbuildinfo
dist-tsnode/

# 用户上传的图片(运行时产物,不进版本库)
backend/uploads/
.env.local

# 前端 build 产物(vite build 输出到此;npm publish 时由 backend 的 prepublishOnly
# 自动 `cd .. && npm run build` 现建,npm pack 经 files:["public"] 打包带走,不入版本库)
backend/public/

# Claude Code 本地个人权限设置(每个开发者/每台机器不同,不进版本库)
.claude/settings.local.json

# 锁文件不入版本库(跨内网/公网环境 lock 的 resolved 源来回变,提交只会反复冲突;
# 各机器用自己 registry 解析生成,版本范围由各 package.json 的依赖声明保证)
package-lock.json
pnpm-lock.yaml

# 看板程序(kanban-code)误把绝对路径当文件名写进项目目录的 hook-events 日志,反复出现
*hook-events.jsonl

# spike 验证脚本(Phase 2 等一次性地基/封装验证用,不入库;提交时手动精确 add 业务代码)
spike/

# 个人知识笔记(与"AI 任务编排"产品定位无关 + 含个人内容 + 增大仓库体积),不进版本库;
# 本地保留(git rm --cached 仅移除索引)。如需共享知识,放 docs/ 并面向产品。
knowledge-base/

# IDE/工具临时目录(ZCode agent 会话、计划、执行日志),不进版本库
.zcode/

# uTools spike 可再生 CSS 产物(downgrade-css.cjs 生成)与临时 Electron 二进制
tools/utools-spike/orig.css
tools/utools-spike/lc108.css
.tmp-eton/
eton-probe/sentinel.txt

# uTools 插件包与包内构建产物(build-upx.ps1 可再生;.upx 同理不入库)
tools/utools-spike/plugin/index.html
tools/utools-spike/plugin/assets/
tools/utools-spike/ai-task-flow.upx
tools/utools-spike/upx-verify/shot-file-mode.png
tools/utools-spike/upx-verify/report.json
